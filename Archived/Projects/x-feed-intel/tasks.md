---
type: task
project: x-feed-intel
domain: software
skill_origin: action-architect
created: 2026-02-23
updated: 2026-02-23
tags:
  - tasks
---

# X Feed Intel — Tasks

## Milestone 0: Foundation & Validation

| ID | Description | State | Depends On | Risk | Acceptance Criteria |
|---|---|---|---|---|---|
| XFI-001 | Scaffold pipeline repo (Node.js/TypeScript — see ADR-001). Create project with package.json, tsconfig, src/ directory structure matching spec components. | done | — | low | `npm install` succeeds; `npx tsc --noEmit` passes; directory layout has src/ subdirs for each component (capture/, attention/, feedback/, shared/) |
| XFI-002 | Create SQLite schema and migration script (posts, cost_log, feedback, topic_weights tables per §7.2; plus digest_item_map table for ID→canonical_id mapping) | done | XFI-001 | low | Migration script creates all 5 tables; schema matches §7.2 exactly plus digest_item_map; `state/pipeline.db` created at pipeline repo root; migration is idempotent; `PRAGMA journal_mode=WAL` enabled in connection setup |
| XFI-003 | Implement topic config loader (parse YAML with global defaults + per-topic overrides, filter resolution per §5.2) | done | XFI-001 | low | Loader parses sample topic config; per-topic `filters` override global default; `max_results` and `max_age_days` defaults apply when topic-level unset; validation rejects malformed config |
| XFI-004 | Implement X API OAuth 2.0 setup and token management (authorization flow, Keychain storage, auto-refresh). Requires X Developer Account — Danny must create account before this task begins. | done ✓ | XFI-001 | medium | OAuth authorization flow completes in browser; tokens stored in macOS Keychain; programmatic token refresh succeeds; refresh failure returns clear error (not silent) |
| XFI-005 | Write X API bookmark benchmark script (one-off pull of ~200 bookmarks, dump to JSON) | done ✓ | XFI-002, XFI-004 | medium | Script pulls bookmarks from X API; output includes post text, author, engagement data; results saved to `benchmarks/bookmarks-YYYY-MM-DD.json`; cost logged |
| XFI-006 | Write TwitterAPI.io benchmark script (pull ~200 results per topic, verify search operators) | done ✓ | XFI-002, XFI-003, XFI-00B | medium | Script runs all configured topic queries; results saved per-topic to `benchmarks/`; operator support (`min_faves:`, `-filter:replies`, `lang:en`) documented; engagement data present in results |
| XFI-007 | Verify API response fields for thread heuristic (in_reply_to_status_id, conversation_id) | done ✓ | XFI-005, XFI-006 | low | Both APIs' thread-relevant fields documented in `docs/api-field-mapping.md`; normalizer heuristic design confirmed or adjusted based on available fields |
| XFI-008 | Create vault directory structure and governance artifacts. **Crumb-governed task** — must run in a Crumb session with vault-check validation, not as a raw filesystem operation. | done | — | low | `_openclaw/feeds/items/`, `_openclaw/feeds/digests/`, `_openclaw/feeds/kb-review/` directories exist; `_openclaw/config/operator_priorities.md` created with proper frontmatter and default content; `type: x-feed-intel` registered in `_system/docs/file-conventions.md` type taxonomy table; vault-check passes |
| XFI-009 | Operator review of benchmark results, topic query refinement, and labeled benchmark set capture | done ✓ | XFI-005, XFI-006, XFI-007 | low | Danny has reviewed benchmark data; topic queries updated in config based on quality assessment; config changes committed; approval recorded in run-log; labeled benchmark set persisted as `benchmarks/xfi-triage-benchmark-YYYYMMDD.json` with operator-assigned quality labels for Phase 2 drift validation |
| XFI-00A | Verify Telegram bot setup and credentials (confirm existing bot works, Tess has API token, bot can send messages and receive replies) | done ✓ | — | low | Telegram bot sends a test message successfully; bot receives reply messages; API token accessible to pipeline process; bot-to-user and user-to-bot paths confirmed working |
| XFI-00B | Set up TwitterAPI.io account and store API key (create account if not exists, purchase credits, store key in Keychain) | done | — | low | TwitterAPI.io account exists with credits loaded; API key stored in macOS Keychain; test API call succeeds (any endpoint); rate limit and pricing tier confirmed |
| XFI-00C | *(Optional)* Build dev/test harness with recorded API responses and fake Telegram sink. Enables offline development and prompt iteration without hitting live APIs or consuming credits. | done ✓ | XFI-005, XFI-006 | low | Recorded API responses saved from benchmark runs (XFI-005/006) as fixture files; dry-run mode flag reads fixtures instead of calling live APIs; fake Telegram sink logs messages to file instead of sending; normalizer, triage engine, and digest can all run in dry-run mode |

## Milestone 1: Capture Clock

| ID | Description | State | Depends On | Risk | Acceptance Criteria |
|---|---|---|---|---|---|
| XFI-010 | Implement normalizer (unified post format per §5.3, canonical_id extraction, source_instances, thread heuristic, matched_topics) | done ✓ | XFI-001, XFI-007 | medium | Normalizer converts X API response to unified format; normalizer converts TwitterAPI.io response to unified format; canonical_id is clean numeric post ID; `needs_context` set by heuristic; `matched_topics` populated from query context |
| XFI-011 | Implement bookmark puller (OAuth-authenticated X API v2 bookmarks, dedup check, failure notification) | done ✓ | XFI-002, XFI-004, XFI-010, XFI-00A | medium | Puller fetches new bookmarks; posts already in DB are skipped; token refresh runs before pull; refresh failure sends Telegram notification; new posts passed to normalizer |
| XFI-012 | Implement topic scanner (TwitterAPI.io queries per config, per-run dedup cache, filter resolution, client-side engagement fallback) | done ✓ | XFI-002, XFI-003, XFI-010, XFI-00B | low | Scanner queries all configured topics; per-run dedup cache prevents same post counted twice within a run; filter string appended to queries; client-side engagement filtering operates when server-side operators unavailable |
| XFI-013 | Implement global dedup engine (canonical_id check against posts table, source_instances update, append-only matched_topics, queue write) | done ✓ | XFI-002, XFI-010 | low | New posts inserted with `queue_status='pending'`; re-encountered posts update `source_instances` and `matched_topics` without re-queuing; already-triaged posts not re-triaged; dedup is atomic (no partial updates) |
| XFI-014 | Implement capture clock scheduling (launchd plists for bookmark puller daily 6 AM + topic scanner per config, retry with exponential backoff, error logging) | done ✓ | XFI-011, XFI-012, XFI-013 | medium | Capture clock fires on schedule via launchd plist at `~/Library/LaunchAgents/ai.openclaw.xfi.capture.plist`; bookmark puller runs daily; topic scanner runs per-topic frequency; retries 3x with 30s/60s/120s backoff on transient failure; each run assigned a unique `run_id` logged with all output; errors logged to `pipeline.log`; partial success handled (one source down, other continues); `runCaptureClock` accepts `force?: boolean` option — when true, `shouldRunScan` returns true regardless of last scan timestamp |

## Milestone 2: Attention Clock

| ID | Description | State | Depends On | Risk | Acceptance Criteria |
|---|---|---|---|---|---|
| XFI-015 | Implement vault snapshot generator (read project frontmatter, operator_priorities, outbox summaries; output vault_snapshot.yaml ≤600 tokens; input fallback rules per §5.5.0) | done ✓ | XFI-008 | low | Snapshot includes active_projects, current_focus_tags, recent_crumb_topics, operator_priorities; output ≤600 tokens; generation completes in ≤2 seconds (prevents accidental O(N vault) implementation); empty outbox → `recent_crumb_topics: []`; malformed project → skip with warning; missing priorities → default string |
| XFI-016 | Implement triage engine in batch mode (Haiku 4.5 LLM call, structured output per §5.5.1 schema, per-post failure isolation) | done ✓ | XFI-002, XFI-013, XFI-015 | high | Engine sends batch of 10-20 posts to Haiku 4.5; response parsed as JSON array with one object per post; all schema fields present; malformed individual results isolated → individual retry → `triage_failed` on persistent failure; triage_json written to posts table |
| XFI-017 | Engineer triage prompt (start from Appendix A v0 skeleton, validate against Phase 0 benchmark sample, iterate 2-3 cycles). This is the highest-risk task — budget 3-6 iterations per Pattern 4 from `claude-print-automation-patterns.md`. | done ✓ | XFI-016, XFI-009 | high | JSON parsing succeeds on 100% of triage outputs; all 8 required schema fields present on 100% of triaged items; no `vault_target` set without `crumb-architecture` tag on any item; no `vault_target` set with `confidence: low`; in a ≥20-post sample, at least 2 distinct confidence levels (low/medium/high) appear; semantic eval: on the labeled benchmark set (from XFI-009), ≥N of 20 items match operator-assigned priority and primary tag; ≥2 iteration cycles completed and documented in run-log |
| XFI-018 | Implement vault router (route crumb-architecture items to `_openclaw/inbox/`, idempotent writes per §5.6, operator notes protection, kb-review staging for save command) | done ✓ | XFI-016, XFI-008 | low | Items meeting routing bar written to `_openclaw/inbox/feed-intel-{id}.md` with correct frontmatter (type: x-feed-intel); re-run on same item updates triage section only; content below `<!-- OPERATOR NOTES BELOW -->` preserved; kb-review files include `save_reason` field |
| XFI-019 | Implement daily digest for Telegram (§5.7 format: high/medium/low sections, item IDs, excerpts, cost footer, degraded mode notes, MAX_ITEMS_INLINE threshold) | done ✓ | XFI-016, XFI-020 | medium | Digest delivered to Telegram with 3 priority sections; each item has alphanumeric ID (A01, B03, etc.); excerpts are first 140 chars (mechanical, not LLM); cost footer shows MTD + projected; degraded mode note appears when upstream source failed; overflow to file digest when >35 items; digest_item_id → canonical_id mapping written to digest_item_map table for each digest run |
| XFI-019b | Persist digest item ID → canonical_id mapping (schema + read/write logic for digest_item_map table, so feedback listener can resolve A01/B03 IDs to posts) | done ✓ | XFI-002, XFI-019 | low | digest_item_map table stores (digest_date, item_id, canonical_id) tuples; XFI-019 writes mappings on each digest run; lookup by (digest_date, item_id) returns canonical_id; old mappings retained for ≥7 days; lookup miss returns clear error (not null) |
| XFI-020 | Implement cost telemetry (per-run cost tracking for bookmark-puller, topic-scanner, triage; MTD aggregation; projected monthly extrapolation; write to cost_log table) | done ✓ | XFI-002 | low | Cost logged per run with component breakdown; MTD aggregation correct across month boundary; projection extrapolation uses current month's daily average; spending cap warning at 80% of X API cap; data available for digest footer |
| XFI-021 | Implement attention clock scheduling (launchd plist for daily 7 AM; chain: queue read → vault snapshot → triage → router → telemetry → digest) | done ✓ | XFI-015, XFI-016, XFI-018, XFI-019, XFI-020 | medium | Attention clock fires daily at 7 AM via launchd plist at `~/Library/LaunchAgents/ai.openclaw.xfi.attention.plist`; full chain executes in sequence; each run assigned a unique `run_id` logged with all output; digest delivered; vault items routed; cost logged; errors in any component don't crash the chain (degraded mode) |

## Milestone 3: Feedback & Operations

| ID | Description | State | Depends On | Risk | Acceptance Criteria |
|---|---|---|---|---|---|
| XFI-022 | Implement feedback listener with launchd daemon (Telegram reply parsing against strict grammar `{ID} {command} [argument]`, reply-to-message-id matching, error responses for malformed/nonexistent/duplicate; KeepAlive launchd plist for persistent polling) | done | XFI-019, XFI-019b, XFI-00A | medium | Valid commands parsed and dispatched; item IDs resolved via digest_item_map lookup; malformed input returns error with format example; nonexistent item ID returns specific error; duplicate command returns idempotency message; `refresh` as standalone (non-reply) message recognized and dispatched to XFI-022b handler; other non-reply messages still ignored; `expand` command recognized but returns "Phase 2 — not yet available" (not a parse error); launchd plist created with `KeepAlive: true` for persistent Telegram polling |
| XFI-022b | Implement `refresh` command — triggers full capture→attention chain on demand via Telegram. Bypasses scan frequency gate (`force: true`). Includes concurrency guard (lockfile at `state/pipeline.lock`, 30-min stale detection). Cost logged with `manual` prefix in run_id. | done | XFI-022, XFI-014, XFI-021 | low | `refresh` message triggers capture+attention chain; scan frequency gate bypassed (`force: true`); immediate acknowledgment sent; digest delivered on completion; error summary on failure; concurrent run detected and rejected with informative message; stale lock (>30 min) treated as stale; cost logged with `manual` prefix in run_id; non-reply messages other than `refresh` still ignored per XFI-022 |
| XFI-023 | Implement promote command with conditional confirmation (check routing bar: crumb-architecture + confidence≥medium → immediate; outside bar → yes/no confirmation) | done | XFI-022, XFI-018 | low | Promote within routing bar executes immediately with confirmation message; promote outside bar sends confirmation prompt; `yes` writes file to inbox; `no` cancels; duplicate promote handled |
| XFI-024 | Implement save, ignore, and add-topic commands (save → kb-review/ staging with save_reason; ignore → feedback log; add-topic → feedback log with argument) | done | XFI-022 | low | `save` writes file to `_openclaw/feeds/kb-review/` with `save_reason: "user-requested via digest"` frontmatter; `ignore` logged to feedback table; `add-topic` logged with topic name argument; all commands logged to feedback table; duplicate save handled |
| XFI-025 | Implement cost guardrail (monitor projected_month vs $6 target; at 90% → reduce max_results 50%; at 80% → revert; digest note when active) | done | XFI-020, XFI-012 | low | Guardrail activates when projected_month ≥ $5.40 (90% of $6); topic scanner max_results halved; reverts when projected_month < $4.80 (80%); digest includes "Cost guardrail active" warning; reduction persists across runs until threshold drops |
| XFI-026 | Implement queue health monitoring and expiry (pending >100 alert; search 7-day / bookmark 30-day expiry; backlog digest at >50 pending; pruning of expired >90 days; expired items remain queryable) | done | XFI-002 | low | Alert sent when pending >100; search-only items expired after 7 days untriaged; bookmark items expired after 30 days; dual-source items use 30-day TTL; backlog summary mode activates at >50 pending; expired items remain in DB with `queue_status='expired'` and are queryable (not silently deleted); records with `queue_status='expired'` older than 90 days pruned; ops guide documents how to query expired items |
| XFI-027 | Implement pipeline liveness check (separate launchd plist, verify successful run in last 24h, Telegram alert on miss) | done | XFI-014, XFI-021 | low | Liveness check runs daily via separate launchd plist; detects no successful capture or attention run in 24h; sends Telegram alert with last-known successful run timestamp |
| XFI-028 | Write runtime operations guide (OAuth re-auth procedure per §10, topic config editing, spending cap adjustment, log locations, common troubleshooting, alert response procedures) | done | XFI-014, XFI-021, XFI-022 | low | Guide at `docs/x-feed-intel-ops.md` covers: (1) OAuth re-auth — all 6 steps from §10 with CLI examples; (2) topic config editing — safe deployment + reload procedure with example YAML; (3) spending cap adjustment; (4) log file locations and rotation; (5) alert response per type: queue backlog (>100), liveness failure (24h gap), cost guardrail activation, source degraded mode; (6) troubleshooting for top 5 failure scenarios from §6.3 with explicit checklist; (7) launchd plist inventory and management (start/stop/verify each process); (8) `refresh` command in Telegram command reference section |
