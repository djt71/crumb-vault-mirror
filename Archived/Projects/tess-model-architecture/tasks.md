---
type: tasks
project: tess-model-architecture
domain: software
skill_origin: action-architect
created: 2026-02-22
updated: 2026-02-22
status: active
---

# Tess Model Architecture — Tasks

| ID | Description | State | Depends On | Risk Level | Domain | Acceptance Criteria |
|----|-------------|-------|------------|------------|--------|---------------------|
| TMA-001 | Write routing specification for two-agent split — define agent configs, channel bindings, fallback chains, pass/fail criteria, and "simpler path" selection criteria table | **done** | — | high | writing | Routing spec exists as standalone doc in `design/`. Pass/fail criteria defined for: two-agent config schema, channel binding (Telegram → tess-voice), pure-type routing (human-clock → cloud, machine-clock → local), mixed-task delegation protocol, fallback chains, bug verification protocol (U12), inter-agent delegation test (U9), mixed-provider validation (U11). "Simpler path" criteria table included (dimensions: agent count, config parameter count, known bug exposure, maintenance surface area). Delegation test scenarios enumerated (persona → mechanic → persona handback, mechanic failure mid-tool, Limited Mode interactions with delegation). |
| TMA-002 | Routing PoC — validate routing mechanism on v2026.2.17. Test if F16 bugs are fixed; if yes, prototype single-agent. Validate two-agent config with channel bindings and mixed providers. Optionally test `modelByChannel` if v2026.2.21 available. | **done** | TMA-001 | critical | code | Minimum 5-scenario test matrix completed: (1) pure persona message routed to cloud model with correct response, (2) pure mechanical task routed to local model with valid structured output, (3) mixed-task delegation or fallback exercised, (4) Limited Mode mechanisms — three distinct verdicts required: (4a) system prompt swap mechanism selected and demonstrated (per TMA-004 §3.2 — config-based, middleware, or conditional append), (4b) tool restriction mechanism selected and demonstrated (per TMA-004 §4.1 — runtime modification, middleware interception, or separate agent config), (4c) recovery mechanism selected and demonstrated (per TMA-004 §6.3 — OpenClaw auto-retry, explicit revert, or health-check cron). Each verdict documents: option tested, pass/fail, evidence, selected mechanism for TMA-008 config. Full Limited Mode scenario also exercised end-to-end: API failure simulated → local fallback activated → degradation banner sent → tool allowlist enforced → auto-recovery on restore, (5) session isolation canary — seeded data in one agent's context not visible to the other. F16 bugs explicitly tested with pass/fail documented. Path selection justified against TMA-001 criteria table. Mixed-provider gateway (Anthropic + Ollama) operational. Environment versions recorded. |
| TMA-003 | Codify design contracts as vault reference doc — standalone MC + PC document usable for future model swaps | **done** | — | low | writing | Standalone doc exists in `design/`. Contains Mechanical Contract (MC-1 through MC-6 with full MC-6 safety specification) and Persona Contract (PC-1 through PC-4 hard gates, PT-1 through PT-4 soft targets). Linked from specification. Usable as evaluation framework for future model swaps without reading the full spec. |
| TMA-004 | Document Limited Mode protocol — triggers, fallback, scope enforcement, recovery | **done** | — | medium | writing | Protocol doc exists in `design/`. Covers: trigger conditions (3x 503/timeout), model swap mechanism, reduced tool allowlist (read-only + capture only), reduced system prompt, no-actuation policy, user notification format (Telegram banner text), auto-recovery logic (5-min health check), duration cap (>4h → operator escalation message), prompt-enforced prohibition on advice voice. Enforcement point specified (router, not model instruction alone). Tool allowlist explicitly enumerated. |
| TMA-005 | Build memory budget table — empirical measurements under defined load shape | **done** | — | medium | research | Actual measurements recorded for qwen3-coder:30b under defined load shape (64K context + concurrent Obsidian + Docker + OpenClaw gateway). Metrics captured: peak RSS, KV cache size at 64K (both q4_0 and q8_0), model load time, swap usage, power draw under sustained load. Stable operation confirmed with no swap activity and ≥15GB free headroom. 80B model promotion gated on measured headroom. Results in `design/` as reference table. |
| TMA-006 | Persona fidelity test — Haiku 4.5 vs Sonnet 4.5 against §9 rubric | **done** | — | medium | research | Both Haiku and Sonnet tested with minimum 5 qualifying cases per PC dimension (≥20 total interactions per model). Hard gates (PC-1 through PC-4): 100% pass across all qualifying cases required. Soft targets (PT-1 through PT-4): scored per dimension, not blocking. Architecture invalidation: if any hard gate fails on both Haiku and Sonnet across ≥3 qualifying cases, invalidation flag raised per §8.2. Decision documented: Haiku only, Sonnet only, or mixed tier. Scores and interaction transcripts recorded in structured format in `design/`. Test categories: boundary-setting, tone-shift judgment, safe ambiguity handling, second-register invocation. |
| TMA-007a | Build benchmark harness — scaffold, test definitions, MC-6 adversarial suite | **done** | — | medium | code | Benchmark harness exists with: 10 tool-call task definitions (spanning OpenClaw's tool inventory), 3 long-context task definitions, MC-6 adversarial suite definitions (replay, paraphrasing, partial echo, unprompted confirmation). Harness is executable but has not yet been run against the model. Test infrastructure validated (can connect to Ollama, submit requests, capture results). |
| TMA-007b | Execute benchmark harness — MC-1 through MC-6 validation on qwen3-coder:30b | **done** | TMA-005, TMA-007a | high | code | Harness executed on qwen3-coder:30b (both Q4_K_M and Q5_K). Recorded: JSON validity rate (≥95% required per MC-1), schema adherence rate (MC-2), median latency (<5s per MC-3), peak memory (MC-4), thermal behavior under sustained 30-min run (no throttling events, CPU temp logged). MC-6 adversarial results: all 4 test types pass. Harness executable via single CLI command with exit code 0 = all MC gates pass, non-zero = failure with failing gate identified. Gate procedure documented: when to run (any model or quantization change), how to interpret results, what to do on failure (block deployment, investigate). Results in `design/`. |
| TMA-008 | Draft `openclaw.json` config — production tiered architecture | **done** | TMA-002, TMA-004, TMA-007b, TMA-010a, TMA-011 | medium | code | Production-ready config exists. Contains: agent definitions per TMA-002 outcome (two-agent or single-agent), model assignments, channel bindings, fallback chains (reflecting TMA-004 Limited Mode protocol), Ollama provider config (`openai-completions` format per TMA-010a), caching config (per TMA-010a `cache_control` results), KV cache quantization (per TMA-007b results). Custom Modelfile with `num_ctx 65536`. Environment variables documented (`OLLAMA_KEEP_ALIVE`, `OLLAMA_KV_CACHE_TYPE`). Persona-tier decision embedded (model mix from TMA-006, Persona Contract references). Config validated against working gateway — each route returns expected model response (smoke test). |
| TMA-009 | Integration test — full tiered routing end-to-end | **done** | TMA-008, TMA-011 | high | code | End-to-end verified: (1) human-clock message → cloud model → persona response with SOUL.md fidelity, (2) machine-clock task → local model → valid structured JSON output, (3) Limited Mode: API failure simulated → local fallback → degradation banner → auto-recovery. Binary checks: no Limited Mode response uses disallowed tools (YES/NO), duration cap enforced in all test runs (YES/NO), state sync verified during Limited Mode (YES/NO). (4) Vault-based state sync — mechanic's actions discoverable by voice via vault/inbox state only, no shared in-gateway memory (A8). Latency measured over ≥20 requests per path (cloud, local, Limited Mode). Method: timestamp at gateway entry and response completion. Pass: p95 <10s cloud, <15s Limited Mode. All routing paths exercised with compressed prompt (TMA-011 output). |
| TMA-010a | API + caching probe — validate Anthropic integration before config | **done** | TMA-001 | high | research | `openai-completions` format confirmed working with Anthropic provider (or `openai-responses` if format issue resolved). `cache_control` passthrough to Anthropic API confirmed OR blocker identified with specific mitigation path (middleware, feature request, or config workaround). If caching unavailable: updated cost model produced ($40/mo uncached Haiku documented) and operator decision on proceed/pivot recorded before Milestone 2. Minimal throwaway config sufficient — does not require full TMA-008 output. Runs in parallel with TMA-002. |
| TMA-010b | Token cost measurement — actual vs projected over representative traffic | **done** | TMA-009 | medium | research | Actual token costs measured over ≥24h representative traffic period. Per-request input/output token counts recorded. Cache hit rate measured. Monthly cost projection calculated from actuals and compared to §11 projections (Haiku: $8.70/mo target, Sonnet: $22.50/mo target). Cost model confirmed within ±20% or revised with evidence. |
| TMA-011 | System prompt optimization — compress SOUL.md for tess-voice | **done** | TMA-006 | medium | writing | Compressed tess-voice system prompt exists (target: 3,500–6,000 tokens, down from ~9,500). Validated against persona rubric — no degradation on Required dimensions (PC-1 through PC-4) compared to uncompressed baseline. tess-mechanic minimal identity doc exists (~200–300 tokens, reliability-focused, no persona). Both docs in `design/`. Token counts measured. |
| TMA-012 | Environment pinning & rollback plan | **done** | — | low | writing | Doc exists in `design/` recording: exact OpenClaw version + build method, Ollama version, Modelfile hash, launchd plist config (`ai.openclaw.gateway`), node/npm versions, current `openclaw.json` snapshot hash, rollback procedure (step-by-step to revert to pre-implementation state). |
