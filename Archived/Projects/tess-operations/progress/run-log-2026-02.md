---
type: run-log
project: tess-operations
domain: software
status: archived
created: 2026-02-26
updated: 2026-02-28
covers: "2026-02-26 through 2026-02-28 — Project creation, SPECIFY (5 specs + peer review), PLAN (action-architect + peer review), TASK M0-M1 (deployment, heartbeat, awareness-check, memory search, browser access, SOUL.md)"
---

# tess-operations — Run Log (Archive: 2026-02)

> **Active run-log:** [[run-log]]

## 2026-02-26 — Project Creation

**Action:** Created project scaffold from inbox-processor intake.

**Context:** Three design documents produced by Tess (claude.ai session) imported from inbox:
- `tess-chief-of-staff-spec.md` — primary capability specification
- `tess-feed-intel-ownership-proposal.md` — feed-intel migration proposal
- `tess-frontier-ideas.md` — exploratory capabilities beyond operational baseline

**Routing decisions:**
- New project created (`tess-operations`) vs. adding to DONE projects (openclaw-colocation, tess-model-architecture)
- Domain: software (system type)
- Phase: SPECIFY — imported specs need Crumb-side validation before planning

**Related projects:** openclaw-colocation (infrastructure), tess-model-architecture (model decisions), feed-intel-framework (pipeline ownership), crumb-tess-bridge (dispatch protocol)

## 2026-02-26 — SPECIFY: Chief-of-Staff Spec Review

**Context inventory:**
- `tess-chief-of-staff-spec.md` — primary spec (490 lines)
- `tess-feed-intel-ownership-proposal.md` — feed-intel migration proposal
- `tess-frontier-ideas.md` — exploratory capabilities
- `openclaw-colocation/project-state.yaml` — DONE, infrastructure baseline
- `crumb-tess-bridge/project-state.yaml` — DONE, bridge operational
- `feed-intel-framework/project-state.yaml` — TASK phase, M2 in progress
- `openclaw-colocation/design/upgrade-v2026-2-24.md` — upgrade runbook (v2026.2.25)
- `_system/docs/overlays/business-advisor.md` — cost/strategy lens
- OpenClaw cron docs (docs.openclaw.ai) — CLI flag verification
- OpenClaw webhook docs (docs.openclaw.ai) — networking architecture

**Review findings (3 must-fix, 4 should-fix):**

Must-fix:
1. CLI flag hallucination risk — **RESOLVED: all flags verified against official docs, no hallucinations found**
2. Webhook networking gap — **RESOLVED: gateway is loopback-only, added polling-first strategy for GitHub events, local hooks for vault/pipeline events**
3. Week 1 gate criteria soft — **RESOLVED: added measurable thresholds (briefing utility ≥3/5, alert false-positive <25%, cost ≤$3/day, stability ≥80% self-heal, ≥1 prompt revision)**

Should-fix:
4. Telegram topic prerequisites — **RESOLVED: added migration steps to §6.2**
5. HEARTBEAT.md mechanics — **RESOLVED: added mechanics section to §4**
6. Missing infrastructure references — **RESOLVED: added §1b with all completed project dependencies**
7. No rollback plan — **RESOLVED: added rollback plan to §13 with selective/full/circuit-breaker tiers**

**Additional amendments:**
- Updated Week 0 from v2026.2.24 to v2026.2.25 (matches peer-reviewed runbook)
- Updated frontmatter: `skill_origin: systems-analyst`, added `status: active`
- Added open question §15.6 for webhook networking upgrade path
- Feed-intel ownership proposal: recommend option 1 (adopt informally, formalize at Phase B)
- Frontier ideas: Dead Man's Switch (#4) should be promoted to Week 1 roadmap

**Decision:** Spec is ready for PLAN phase after operator review of amendments.

## 2026-02-26 — SPECIFY: Google Services Spec Review

**Context inventory:**
- `_inbox/tess-google-services-spec.md` — new spec from Tess (467 lines)
- Chief-of-staff spec (amended earlier this session) — parent spec
- OpenClaw colocation project state — credential isolation context
- gogcli docs (github.com/steipete/gogcli) — CLI verification
- Business Advisor overlay — cost/risk lens

**Review findings (1 blocking, 3 must-fix, 5 should-fix):**

Blocking:
1. OAuth token + user isolation conflict — **RESOLVED: file backend + `--manual` auth flow. Token encrypted in openclaw user's config dir, `GOG_KEYRING_PASSWORD` env var for unattended access.**

Must-fix:
2. gogcli command verification — **RESOLVED: 11/15 commands correct, 4 corrected (thread modify syntax, calendar --days flag, contacts subcommand, Drive mkdir sequential calls)**
3. Missing failure modes table — **RESOLVED: added §10 with 10 failure modes and mitigations**
4. Missing cost analysis — **RESOLVED: added §11, incremental cost $2.50-6/month**

Should-fix:
5. Drive `drive.file` scope inconsistency — **RESOLVED: added to failure modes table, explicit upgrade path at Phase 4**
6. Audit log resilience — **RESOLVED: local fallback in `_openclaw/logs/google-audit.log`**
7. Stale staging hold cleanup — **RESOLVED: 48-hour auto-delete in failure modes table**
8. Version reference v2026.2.24 → v2026.2.25 — **RESOLVED**
9. Frontmatter missing `status`, `skill_origin` — **RESOLVED**

**Filed to:** `Projects/tess-operations/design/tess-google-services-spec.md`

### Session-End Compound

**Conventions confirmed:**
- CLI flag verification pass is mandatory for any spec referencing external tool commands — now proven across two specs in a single session (OpenClaw cron: clean; gogcli: 4 corrections). Pattern is stable.
- Business Advisor overlay fires correctly on specs with cost/resource/strategic dimensions.

**Patterns observed:**
- Tess-originated specs are high quality but consistently missing: failure modes tables, cost analysis sections, and infrastructure prerequisite awareness (loopback networking, user isolation). These are Crumb's value-add during SPECIFY validation.
- Cross-user credential access is a recurring integration challenge on the Studio (Keychain per-user scoping). File-backend + env var is the generalizable pattern — may apply to future tool integrations beyond gogcli.

**Routing:**
- Cross-user credential pattern → candidate for `_system/docs/solutions/` if it recurs in a third context
- "Tess specs need Crumb review checklist" → not yet formalized, but the 7-item review template (CLI verification, networking, gate criteria, failure modes, cost, rollback, infrastructure refs) could become a reusable checklist if tess-operations continues producing specs at this rate

## 2026-02-26 — SPECIFY: Apple Services Spec Review

**Context inventory:**
- `_inbox/tess-apple-services-spec.md` — new spec from Tess (610 lines)
- Chief-of-staff spec (amended earlier) — parent spec
- Google services spec (amended earlier) — sibling spec
- OpenClaw colocation project state — infrastructure baseline
- Business Advisor overlay — cost/strategy lens
- remindctl GitHub docs, memo GitHub docs — CLI verification sources

**Review findings (1 blocking, 3 must-fix, 5 should-fix):**

Blocking:
1. Cross-user execution model not addressed — **RESOLVED: added §2.3 with tiered execution strategy (sudoers + wrapper script + per-mechanism approach for EventKit vs. AppleScript vs. filesystem). Phase 0 prerequisites updated with cross-user setup steps before TCC grants.**

Must-fix:
2. No failure modes table — **RESOLVED: added §10 with 12 failure modes covering cross-user execution, TCC drift, Reminders triage accuracy, Notes data loss, calendar de-duplication, Contacts timeout, iMessage prompt injection, iCloud eviction, Shortcuts failure.**
3. No cost analysis — **RESOLVED: added §11, incremental cost $1-2.50/month (lower than Google services because Apple operations are read-heavy with less LLM synthesis).**
4. CLI flag verification — **RESOLVED: verified remindctl (12/12 correct) and memo (7/7 correct) against GitHub docs. Fixed icalBuddy `-f` flag (doesn't exist — corrected to `-npn -nc` for clean output). `imsg` flagged for Phase 3 verification.**

Should-fix:
5. Frontmatter missing `status`, `skill_origin` — **RESOLVED**
6. Vault path wrong (`~/obsidian-vault/` → `~/crumb-vault/`) — **RESOLVED**
7. Gate criteria not measurable — **RESOLVED: added quantitative thresholds to Phase 1-3 gates (utility ≥3/5, routing accuracy ≥80%, false-positive <20%, zero TCC failures, wrapper success ≥95%)**
8. `calctl` mentioned but undefined — **RESOLVED: clarified as OpenClaw skill wrapper, not standalone CLI. Updated §2.1 table.**
9. No rollback plan — **RESOLVED: added §12 with selective/full/circuit-breaker tiers. Circuit breaker is single-point: rename wrapper script to disable all Apple operations.**

**Additional amendments:**
- Updated §7.5 TCC health check to use cross-user wrapper pattern
- Updated §9 heartbeat to reference wrapper-based health check
- Updated frontmatter: `skill_origin: systems-analyst`, added `status: active`

**Filed to:** `Projects/tess-operations/design/tess-apple-services-spec.md`

**Decision:** Third sibling spec reviewed and amended. All three tess-operations specs (chief-of-staff, Google services, Apple services) are now validated. The cross-user execution model in §2.3 is a new architectural component — recommend it be validated during Phase 0 implementation before proceeding to Phase 1 of either Google or Apple services.

## 2026-02-26 — SPECIFY: Communications Channel Spec Review

**Context inventory:**
- `_inbox/Tess-comms-channel-spec.md` — new spec from Tess (802 lines)
- Chief-of-staff spec, Google services spec, Apple services spec — cross-reference
- OpenClaw CLI v2026.2.25 `--help` output — command verification (all subcommands verified via `openclaw` user)
- OpenClaw Discord docs (docs.openclaw.ai) — config pattern verification
- tess-model-architecture project state — model decision cross-check
- `crossContextRoutes` issue #22725 — confirmed real on GitHub

**Review findings (3 must-fix, 6 should-fix):**

Must-fix:
1. Tess model wrong — §4.1 said "Claude Sonnet 4.5 (primary)". **RESOLVED: corrected to "Haiku 4.5 (voice agent)" per tess-model-architecture and all sibling specs.**
2. No cost analysis — **RESOLVED: added §11, incremental cost $0.90-1.80/month. Lowest of all sibling specs — most Discord operations are direct API calls with no LLM processing.**
3. CLI flag corrections — `openclaw channels status --channel discord` doesn't exist (corrected to `--probe`); `openclaw pairing list discord` doesn't take channel argument (corrected). **RESOLVED: both fixed in Appendix B.**

Should-fix:
4. Frontmatter missing `status`, `skill_origin` — **RESOLVED**
5. No rollback plan — **RESOLVED: added §12 with selective (Discord-only) and circuit breaker (single config flag) tiers.**
6. Failure modes table thin (5 → 10) — **RESOLVED: added forum thread creation failure, identity link misconfiguration, inline button callback timeout, message chunking content loss, cross-context webhook failure.**
7. Phase 2-3 gate criteria not measurable — **RESOLVED: added quantitative thresholds (≥90% archive coverage, ≥95% delivery success, ≥99% bot uptime, approval mirror within 30 seconds).**
8. Multi-account Discord config unverifiable — **RESOLVED: added verification note in §4.2 flagging that multi-account pattern needs Phase 0 confirmation against v2026.2.25.**
9. No infrastructure prerequisites — **RESOLVED: added §1b referencing colocation, tess-model-architecture, existing Telegram bot, OpenClaw version requirement, and crumb-tess-bridge.**

**Filed to:** `Projects/tess-operations/design/tess-comms-channel-spec.md`

**CLI verification result:** All 7 OpenClaw CLI commands verified against `--help` output (message send, message thread create, config set, gateway restart, channels status, pairing list, pairing approve). Two flag corrections needed (channels status, pairing list). Issue #22725 (crossContextRoutes) confirmed real on GitHub. Overall CLI quality much better than Apple services spec — only 2 minor flag issues vs. 0 hallucinated commands.

**Decision:** Fourth and final sibling spec reviewed and amended. All four tess-operations specs are now validated:
- Chief-of-staff (parent) — 3 must-fix, 4 should-fix → all resolved
- Google services — 1 blocking, 3 must-fix, 5 should-fix → all resolved
- Apple services — 1 blocking, 3 must-fix, 5 should-fix → all resolved
- Communications channel — 3 must-fix, 6 should-fix → all resolved

SPECIFY phase is complete for the current spec family. Project is ready for PLAN phase.

## 2026-02-26 — SPECIFY: Peer Review — Google Services + Comms Channel Specs

**Context:** Peer review panel dispatched for two specs (Google services, comms channel). 4 reviewers per spec: OpenAI GPT-5.2, Google Gemini 3 Pro Preview, DeepSeek Reasoner V3.2, Grok 4.1 Fast Reasoning.

### Google Services Spec — Peer Review Results

**Reviewers:** All 4 succeeded, 60 total findings.

**Must-fix (3) — applied:**
- A1: Drive `drive.file` scope contradicts "Read all Drive files" in §4.1 — 3 reviewers caught. Fixed: narrowed governance to match actual scope, added Phase 4 upgrade note.
- A2: §7.3 Keychain reference contradicts §2.5 file backend — 3 reviewers caught. Fixed: replaced with file keyring reference.
- A3: Gmail filter race condition — Gemini caught that parallel filter execution lets `@Risk/High` emails also match `@Agent/IN`. Fixed: added `-label:@Risk/High` exclusion to all triage queries in §4.1 and §9.

**Should-fix (6) — applied:**
- A4: GOG_KEYRING_PASSWORD hardening — 3 reviewers flagged plain env var risk. Fixed: added Keychain retrieval pattern via `security find-generic-password`.
- A5: Approval timeout — Grok caught missing timeout for Danny's absence. Fixed: added 48-hour auto-expire to `@Agent/OUT`.
- A6: Auth flow UX — Grok caught missing procedure for cross-user OAuth. Fixed: added explicit `sudo -u openclaw` procedure to Phase 0.
- A7: Verbatim email snippet in approvals — OpenAI caught that summaries could sanitize red flags. Fixed: added original context field to approval format.
- A8: Calendar invite trust predicate — OpenAI caught vague criteria. Fixed: added exact email match + no-external-attendees requirement.
- A9: Drive quota failure mode — Grok caught missing row. Fixed: added quota exhaustion row to §10.

**Declined:** 11 findings (overkill, out-of-scope, incorrect, constraint).

**Review note:** `Projects/tess-operations/reviews/2026-02-26-tess-google-services-spec.md`

### Communications Channel Spec — Peer Review Results

**Reviewers:** 3 of 4 valid — DeepSeek reviewed the wrong artifact (Google services content instead of comms channel). Excluded from synthesis.

**Must-fix (3) — applied:**
- A1: Approval mirror message ID persistence — all 3 valid reviewers caught (Gemini rated CRITICAL). No mechanism to correlate Discord message ID with Telegram approval for edit-in-place. Fixed: added approval record schema with message ID correlation and reply-based fallback.
- A2: Audit-log channel routing gap — Grok caught that mechanic-bot's config excludes `#audit-log` despite audit logs coming from multiple agents. Fixed: added `#audit-log` to mechanic-bot config and S4.1 table.
- A3: Telegram-down fallback contradicts architecture — Grok caught that Discord DM as Telegram fallback contradicts S2.3 "unstable for interaction". Fixed: replaced with queue-based approval deferral, emoji-reaction interim UX, and explicit "no autonomous actions during Telegram outage" policy.

**Should-fix (6) — applied:**
- A4: Phase 0 config schema validation — 3 reviewers flagged config keys may be silently ignored. Fixed: added config validation step with effective-config verification.
- A5: Elevate local bridge as primary cross-context mechanism — 3 reviewers flagged crossContextRoutes dependency on unmerged issue #22725. Fixed: promoted local bridge (shared-secret auth, idempotency keys, disk queue) to primary; demoted crossContextRoutes to "future enhancement."
- A6: Application-level Discord heartbeat — 2 reviewers flagged zombie connection risk. Fixed: added canary message pattern to §7.3 for zombie detection.
- A7: Human account hardening — OpenAI caught missing security for Danny's Discord account. Fixed: added §8.1b with 2FA, invite control, periodic member audit.
- A8: Minimize bot intents — 2 reviewers flagged broader-than-needed privileged intents. Fixed: updated §7.2 and §4.3 with per-bot minimum intent profiles.
- A9: Discord ToS/data retention — Grok caught missing data retention and ToS note. Fixed: added data retention and ToS compliance notes to §8.3.

**Declined:** 12 findings (overkill, out-of-scope, constraint, incorrect).

**Review note:** `Projects/tess-operations/reviews/2026-02-26-tess-comms-channel-spec.md`

### Peer Review Observations

- DeepSeek reviewing the wrong artifact is a dispatch reliability issue — the prompt included the correct file path but DeepSeek's response was entirely about Google services. Likely a model-side context confusion rather than a dispatch issue (the raw JSON confirms the correct prompt was sent). Monitor for recurrence.
- Grok produced the most unique actionable insights (Telegram fallback contradiction, audit-log routing gap, forum thread limits). Consistent with prior calibration notes — Grok's adversarial read-through finds operational contradictions others miss.
- Gemini's CRITICAL rating on the approval mirror message ID persistence was the highest-signal finding across both reviews. Its precision on the Gmail filter race condition (query-level fix, not inbox-hiding) was also notably better than DeepSeek's recommendation on the same topic.
- The 7-item Crumb review checklist (CLI verification, networking, gate criteria, failure modes, cost, rollback, infrastructure refs) caught most structural issues, but peer review found internal consistency gaps and security hardening opportunities that the checklist doesn't cover. Peer review adds clear value for specs of this complexity.

### Session-End Compound

**Conventions confirmed:**
- Peer review adds measurable value beyond Crumb's review checklist for specs of this complexity. The Gmail filter race condition (query-level exclusion) and approval mirror message ID persistence were both genuine gaps that the checklist doesn't cover. Pattern is stable — recommend peer review for any spec with cross-system integration points.
- DeepSeek artifact confusion is a new failure mode — the correct prompt was sent but the model responded with content from a different spec. Candidate for `_system/docs/failure-log.md` if it recurs.

**Patterns observed:**
- Grok consistently produces the most operationally adversarial findings (Telegram fallback contradiction, audit-log routing gap). Its addendum calibration continues to work well.
- Gemini excels at precise, actionable fixes (Gmail filter race condition: query-level exclusion, not inbox-hiding; approval mirror: CRITICAL rating with specific KV store proposal).
- Cross-spec consistency issues (Keychain vs. file backend, model names) are caught by external reviewers more reliably than by Crumb's single-pass review — triangulation adds value.

**Routing:**
- DeepSeek wrong-artifact issue → if it recurs in a third context, candidate for `_system/docs/solutions/`
- "Crumb review checklist + peer review for integration specs" → candidate for formalization if tess-operations continues to validate this two-pass pattern

## 2026-02-26 — SPECIFY: Final Peer Review Synthesis — Amendments Applied

**Context:** Peer review synthesis from Tess (claude.ai session) covering all four specs. Reviewed by DeepSeek, Gemini, ChatGPT, Perplexity. Synthesis pre-filtered by Tess with hallucination quarantine.

**Review quality:** ChatGPT and Perplexity rated high signal. DeepSeek medium-high (hallucinated tool references). Gemini low (fabricated CVE, generic advice).

**Context inventory:**
- `_inbox/peer-review-synthesis.md` — synthesis document (256 lines)
- All four tess-operations specs — cross-reference targets
- Previous peer review notes — consistency check

### Amendments Applied (11 total)

**Tier 1 — Structural gaps (4):**
1. **Standardized Approval Contract** — Added §9b to chief-of-staff spec. Canonical schema (approval_id, action_type, service, target, summary, original_context, risk_level, preview, expires_at), timeout behavior, anti-spam batching (>3 pending → bundle), unified audit logging, cooldown defaults.
2. **Cross-Spec MVP Matrix** — Added to chief-of-staff §14. Table showing MVP scope per spec, activation sequencing (Week 1 gate → sibling Phase 1s).
3. **Global Kill-Switch** — Added to chief-of-staff §13 as tier 0. `~/.openclaw/maintenance` file flag, checked by all entry points, one-command halt/resume.
4. **`-label:@Risk/High` Invariant** — Strengthened in Google spec §4.1. Promoted from design note to named invariant with mandate to embed in wrapper scripts, not individual queries.

**Tier 2 — Hardening (7):**
5. **Email Send Technical Enforcement** — Added §7.4 to Google spec. Wrapper-level gate blocking `gog gmail send` without valid AID-* token, security event logging.
6. **Per-Job Token Budgets** — Added table to chief-of-staff §11. 7 job types with max tokens/run and truncation behavior.
7. **HEARTBEAT.md Scope Cap** — Added to chief-of-staff §4. 10-entry max per agent, monthly review, add-one-remove-one rule.
8. **Discord Availability Rule** — Added to comms spec §2.3. Explicit statement: Discord down → operations unaffected, no approvals blocked.
9. **gogcli Vendor Risk** — Enhanced Google spec §10 failure mode row. Single-maintainer risk called out, fallback ladder (native OpenClaw skills → direct curl).
10. **Drive Scope Escalation Trigger** — Enhanced Google spec §8 Phase 4 checklist item. Concrete decision criteria: only upgrade if concrete use case can't work with `drive.file`.
11. **Notes Export Volume Guardrail** — Added to Apple spec §3.2. One-at-a-time, operator-initiated. No bulk export. Large migrations become dedicated tasks.

**Tier 3 — Deferred (6):** Formal threat model, operational SLOs, spec index, dedicated Google account, model fallback policy, per-capability acceptance criteria. All correctly assessed as premature or PLAN-phase work. Alerting-rules reframe from 3.2 to be incorporated into failure modes tables during PLAN.

**Unverified claims quarantined:** 7 items (bbgun, issue #10872, CVE-2025-43530, fabricated config params, BlueBubbles recommendation). To be validated only when relevant features reach implementation.

**Filed to:** `Projects/tess-operations/reviews/2026-02-26-peer-review-synthesis.md`

**Decision:** All actionable findings applied. SPECIFY phase remains complete. Specs are strengthened for PLAN transition.

### Phase Transition: SPECIFY → PLAN
- Date: 2026-02-26
- SPECIFY phase outputs: 4 validated specs (chief-of-staff, Google services, Apple services, comms channel), 3 review notes, 2 supplementary docs (feed-intel ownership proposal, frontier ideas), specification-summary.md
- Compound: Prior session-end compounds covered all key insights — Tess spec review checklist pattern (7-item), cross-user credential pattern, peer review value for integration specs, Grok adversarial calibration, Gemini precision. No new compoundable insights from the transition itself.
- Context usage before checkpoint: fresh session (prior session ended with commit)
- Action taken: none needed (fresh session)
- Key artifacts for PLAN phase: `design/specification-summary.md`, project-state.yaml

## 2026-02-26 — PLAN: Frontier Ideas Peer Review Integration

**Context inventory:**
- `_inbox/frontier-ideas-review-synthesis.md` — Tess-dispatched synthesis (361 lines, 4 reviewers: DeepSeek, Gemini, ChatGPT, Perplexity)
- `design/tess-frontier-ideas.md` — existing frontier ideas doc (219 lines)
- `design/tess-chief-of-staff-spec.md` — parent spec (for promoted ideas)

**Review quality:** ChatGPT and Perplexity rated high signal. DeepSeek medium-high. Gemini medium (one privacy overreach proposal, shallow implementation advice).

**Changes applied:**

Promotions to chief-of-staff §14:
- Anticipatory Session → Week 2+ item #5 with context injection schema (sections, max tokens, wikilinks)
- Dead Man's Switch → Week 0 prerequisite with dual-signal monitoring (gateway + job-ran)

Existing ideas amended (6):
- Adversarial Pre-Brief — source-backed output format (Facts cited / Hypotheses labeled / Recommended Responses), dependency on competitive intel pipeline
- Three-Body Protocol — acknowledged asymmetry (Claude→Vault→Tess automated, reverse is manual), added handoff classification (actionable/reference/spec-impacting)
- Parallel Research Swarm — capped at 3-5 subagents per swarm calibration evidence
- Session Recorder — manual pilot first approach, strict schema, preview before KB promotion
- Attention Flywheel — hard constraints (single metric, single feedback point, diversity slot)
- Knowledge Arbitrage — hard constraints (opt-in, curated 5-10 models, 1 per brief max, kill criteria)

Reclassified: Voice Capture (#9) → DEFERRED with existing-tool pivot (Voice Memos → iCloud, Drafts app)

New ideas added (6):
- Follow-Through Engine [DESIGN-NEEDED] — post-meeting commitment loop (capture → generate → close)
- Relationship / Cadence Tracking [DESIGN-NEEDED] — lightweight keep-warm layer for ~25 accounts
- Proactive Skill Drafting [DESIGN-NEEDED] — Tess self-identifies repeatable patterns, drafts skills for review
- Fact-Checking Sub-Agent [DESIGN-NEEDED] — validates claims in outbound content before send
- Session Compaction [EXPLORATORY] — persistent project memory that condenses over time
- Secrets & Privacy Hygiene [Standing Capability] — automated secret/PII scanning on outbound content

Structural improvements:
- Added readiness rubric with explicit tier criteria
- Added noise budget requirement per idea
- Added revenue-critical vs operational vs personal classification
- Added swarm calibration appendix

Rejected proposals (6): AI Colleague Interface, Consulting Arbitrage, Vibe Check, Adversarial Mirror/Phishing Firewall, Tournament-based Elo ranking, Lobster-mandatory. All correctly excluded.

**Filed synthesis to:** `reviews/2026-02-26-frontier-ideas-review-synthesis.md`

## 2026-02-26 — PLAN: Post-Synthesis Claim Verification + Apple Spec Updates

**Context:** Tess post-synthesis research resolved all previously unverified claims from the operational spec peer review.

**Verified claims — moved to Confirmed Real:**
- `directPolicy` heartbeat config (v2026.2.25) — confirmed
- `strictTransportSecurity` gateway security headers — confirmed
- CVE-2025-43530 (TCC bypass via AppleScript/VoiceOver) — real, patched macOS 26.2
- BlueBubbles as recommended iMessage path — confirmed in OpenClaw docs (`imsg` deprecated)

**Confirmed hallucinated:**
- `bbgun` npm package — does not exist
- "Typed Deterministic Workflows" — fabricated buzzword

**Deprioritized:** OpenClaw issue #10872 — `imsg`-specific, moot since BlueBubbles is the path

**Apple spec §3.5 updated:** BlueBubbles now primary integration (was `imsg` or BlueBubbles). Tool inventory, integration approach, Phase 3 checklist, and open question #3 all updated.

**Apple spec Phase 0 updated:** Added macOS 26.2+ verification prerequisite (CVE-2025-43530 mitigation).

**Operational spec synthesis updated:** "Unverified Claims" section replaced with "Verified Claims — Post-Synthesis Research" with confirmed/hallucinated/deprioritized classifications.

## 2026-02-26 — PLAN: Feed-Intel Ownership Proposal — Peer Review Integration

**Context inventory:**
- `_inbox/feed-intel-ownership-review-synthesis.md` — Tess-dispatched synthesis (220 lines, 4 reviewers: DeepSeek, Gemini, ChatGPT, Perplexity)
- `design/tess-feed-intel-ownership-proposal.md` — ownership proposal (313 lines)

**Review quality:** ChatGPT and Perplexity rated high signal. DeepSeek medium-high (credential access nuance, phasing). Gemini low-medium (isolated-session context gap was useful, but webhook recommendation unsupported and invented "Session Paradox" terminology).

**All four reviewers recommend adoption.** Disagreements are on implementation details, not direction.

**Changes applied — Tier 1 (5 structural gaps):**
1. **Runtime Config Contract** (§3.6) — formalized config source hierarchy (5 source types), validation on parse failure (fallback to last-good cache), enforcement rule against hand-editing cron definitions
2. **Telegram Delivery Mechanics** (§3.2 rewrite) — replaced bare `--message` patterns with explicit JSON payloads including `sessionTarget: "isolated"`, `payload.kind: "agentTurn"`, and `delivery` blocks
3. **Feedback Verb Contract** (§3.7) — defined 5-verb DSL (investigate, ignore, more, mute, deeper) with input format, action, routing, and log entry per verb
4. **Machine-Checkable Health Signals** (§5 rewrite) — replaced vague "check status" with 7 named signals (`last_capture_run_at`, `last_attention_run_at`, `queue_depth`, `last_successful_delivery_at`, `last_processed_feedback_at`, `error_rate_by_adapter`, `daily_token_spend`) with specific thresholds
5. **Cron Guardrails and Failure Handling** (§3.8) — locking, wall time limits, missed-run catch-up policy, 4 degraded state definitions with auto-pause at 3 consecutive failures

**Changes applied — Tier 2 (5 hardening items):**
6. **Chief-of-Staff Gate Dependency** (§6) — Phase B begins only after chief-of-staff §14 Week 1 gate (5 days stable)
7. **Cost Caps** (§3.9) — per-run token ceilings, $1.50/day hard cap, mandatory weekly cost report
8. **Heartbeat Polling as Feedback Default** (§3.2) — resolved open question #1, documented upgrade path to webhook
9. **Morning Briefing Context Injection** (§5) — explicit requirement for briefing to read latest digest file + status line
10. **Credential Access Detail** (§3.4 expansion) — cron job keychain access, rotation detection, secret hygiene

**Resolved open questions:** #1 (feedback listener → heartbeat polling), #2 (Lobster → raw cron, Lobster deferred), #3 (x-feed-intel → ARCHIVED after Phase B)

**Filed synthesis to:** `reviews/2026-02-26-feed-intel-ownership-review-synthesis.md`

## 2026-02-26 — PLAN: Cross-Spec Consistency — Chief-of-Staff Propagation

**Context:** Peer review synthesis amendments were applied to sibling/supplementary specs but cross-spec implications weren't propagated back to the chief-of-staff master spec. Scanned all three synthesis action sequences for pre-PLAN items targeting the chief-of-staff spec.

**Changes applied to chief-of-staff spec:**

1. **§5.1 Morning Briefing** — Added feed-intel status line (post-Phase B). Morning briefing must explicitly read feed-intel digest file because triage runs in `--session isolated`. Added as a distinct briefing content item with cross-reference to ownership proposal §5.

2. **§11 Cost Analysis** — Added feed-intel operations row (~$45/month, Phase B+). Updated projected total from $30-70 (chief-of-staff only) to $75-115 (combined). Added note that feed-intel cost is governed separately with its own $1.50/day hard cap.

3. **§13 Failure Modes** — Added Alert Rule column to failure modes table. Defined specific alerting thresholds: 2 consecutive briefing failures → Telegram alert, failed restarts → immediate alert, per-job ceiling breach → alert + kill, research staleness → weekly flag. Addresses operational synthesis Tier 3.2 recommendation ("alerting rules, not SLOs"). Updated rollback cost ceiling from $70 to $120 to account for combined chief-of-staff + feed-intel costs.

4. **§14 Week 2+ item #10** — Added explicit Week 1 gate prerequisite for feed-intel ownership transition, with cross-references to ownership proposal §6 (phasing) and §3.6-3.9 (runtime contracts).

5. **§4 Mechanic HEARTBEAT.md** — Added post-ownership-transition note documenting the evolution from "3 pipeline launchd services" to the 7 machine-checkable health signals defined in the ownership proposal §5.

### Session-End Compound

**Conventions confirmed:**
- Cross-spec consistency checks should be run after amending any supplementary doc with implications for the master spec. The feed-intel ownership proposal was amended with 10 items but none were propagated to the chief-of-staff spec until an explicit pass was run. This is a repeatable pattern for multi-spec projects.
- Peer review synthesis "before PLAN" action sequences are reliable checklists — all items from three syntheses were traced and verified. The synthesis format with numbered action items and explicit tier classification (Tier 1/2/3) makes audit straightforward.

**Patterns observed:**
- Cost ceiling propagation is easy to miss. Feed-intel adds ~$45/month, pushing the combined total above the original $70 rollback trigger. The rollback trigger in §13 needed updating to $120 — this kind of secondary effect doesn't surface unless you explicitly check the master spec's cost/limit sections against all subsidiary cost commitments.
- Alerting rules (not SLOs) are the right abstraction for the failure modes table at this phase. Numeric SLOs require operational data; alerting rules require only thresholds and routing decisions, which can be designed now.

**Routing:** Cross-spec consistency check as a post-amendment step — candidate for formalization if multi-spec projects recur. Currently a manual step triggered by operator observation.

## 2026-02-26 — PLAN: Action-Architect — Milestone & Task Decomposition

**Context inventory (extended tier — 7 docs, justified by 4-spec software project):**
- `design/specification-summary.md` — compressed overview of all 4 specs + sequencing (139 lines)
- `design/tess-chief-of-staff-spec.md` — targeted: prerequisites §14 Week 0, constraints §9, gate criteria §14, cost §11, rollback §13, HEARTBEAT §4
- `design/tess-google-services-spec.md` — targeted: Phase 0 §8, constraints §4, gate criteria, cost §11, failure modes §10
- `design/tess-apple-services-spec.md` — targeted: Phase 0 §8, cross-user execution §2.3, constraints §4, gate criteria, cost §11, rollback §12
- `design/tess-comms-channel-spec.md` — targeted: Phase 0 §9, constraints §2.3/§8/§10.2, gate criteria, cost §11, rollback §12
- `_system/docs/solutions/claude-print-automation-patterns.md` — dispatch automation patterns (Pattern 4: budget 3-6 iterations for live deployment)
- `_system/docs/overlays/business-advisor.md` — cost/resource allocation lens

**Overlay activated:** Business Advisor — cost envelope drives milestone scoping and gate sequencing.

**Key planning constraints identified:**
1. Week 1 gate is the critical path — all sibling specs blocked until 5-day evaluation passes
2. Phase 0s for Google/Apple/Comms can run in parallel AFTER Week 1 gate
3. Apple Phase 4 (iMessage send) blocked on Google Phase 3 (email send) being stable 2+ weeks
4. Feed-intel ownership transition blocked on Week 1 gate
5. Per-job token budgets and global kill-switch are Week 0 infrastructure
6. Cross-user execution (sudo wrapper + TCC) is Apple's blocking prerequisite
7. Live deployment iteration budget: 3-6 iterations per new dispatch pattern (from solutions doc)

**Deliverables produced:**
- `action-plan.md` — 9 milestones (M0–M8), dependency graph, rollback tiers, Business Advisor cost analysis
- `tasks.md` — 48 atomic tasks (TOP-001 through TOP-048), dependency graph, risk distribution (9 high, 21 medium, 18 low)
- `action-plan-summary.md` — milestone map, critical path, parallel execution windows, cost envelope, high-risk task inventory

**Architecture decisions in the plan:**
- Milestone boundaries align with spec phase boundaries — no cross-cutting milestones that mix phases from different specs
- Gate evaluations are explicit tasks (TOP-014, TOP-030, TOP-035, TOP-041) that block subsequent milestones
- Intelligence layer (M8) and feed-intel monitoring (M7.1) can start in parallel with service integrations after Week 1 gate — they don't depend on Google/Apple/Discord infrastructure
- iMessage send (TOP-043) has the longest dependency chain: M0 → M1 → M2 → M3 → M4 → M5 → M6, plus a 2-week stabilization window after Google email send

**Peer review assessment:** HIGH impact — this plan drives all implementation for 4 interconnected specs with cross-user auth, security-sensitive write operations, and external service dependencies. Recommend peer review before execution.

**Also fixed:** Peer review synthesis doc (`reviews/2026-02-26-peer-review-synthesis.md`) — restored verification source attribution for Confirmed Real table (CVE-2025-43530 audit trail) and expanded Recommended Action Sequence to reflect completed items (items 4-5 were collapsed during Crumb integration).

## 2026-02-26 — PLAN: Action Plan Peer Review + Amendments

**Context:** Peer review panel (GPT-5.2, Gemini 3 Pro Preview, DeepSeek V3.2, Grok 4.1 Fast) reviewed `action-plan.md`. 4/4 reviewers succeeded. 60 findings total.

**Review note:** `reviews/2026-02-26-action-plan.md`

**Must-fix (3) — applied:**
- A1: M1 morning briefing listed "calendar" and "reminders" but integration isn't built until M3. Removed from Week 1 scope.
- A2: "FIF M2" prerequisite was an opaque abbreviation. Expanded with explicit project reference to `Projects/feed-intel-framework/`.
- A3: Critical path didn't show the 2-week stabilization hold between email send (M5.1) and iMessage send (M6.2). Added explicitly with 8-12 week total timeline estimate.

**Should-fix (8) — applied:**
- A4: Added spec traceability appendix mapping spec phases → milestones → task IDs.
- A5: Every milestone phase now references specific TOP-xxx task IDs in its heading.
- A6: Added GUI canary test to TOP-022 acceptance criteria (AppleScript + memo via wrapper).
- A7: Operationalized subjective gate criteria — "useful" replaced with "operator marks 0/1 per day" scoring.
- A8: Expanded rollback tiers: Tier 0 adds manual process kill, Tier 4 adds incremental re-enablement. Added per-service mismutation remediation table.
- A9: Added alert dedup/cooldown policy to TOP-013 (one per condition per 30 min, escalation ladder).
- A10: Specified Dead Man's Switch failure condition (2 hours) and action (SMS/push, NOT auto-restart) in TOP-003.
- A11: Added feed-intel isolation note to M7.2 (separate session, cost cap, pause flag).

**Declined (13):** Misunderstandings of two-artifact design (action-plan + tasks.md), internal project context reviewers can't verify, implementation-level detail below plan scope.

**Reviewer calibration observations:**
- Gemini produced the single highest-signal finding (M1 calendar dependency gap) with the fewest total findings (7). Precision over volume.
- Grok's critical path observation about the 2-week hold was the second most valuable unique finding.
- OpenAI produced the deepest analysis (23 findings) but several were already addressed in the specs or were implementation detail. High recall, moderate precision.
- DeepSeek was medium signal — valid but largely overlapping with other reviewers.
- All 4 reviewers flagged unverifiable claims (CVE, OpenClaw version, etc.) — expected given they lack access to internal project context. These were pre-verified during SPECIFY phase.

**Decision:** Action plan peer-reviewed and amended. Plan is ready for PLAN → TASK phase transition.

## 2026-02-27 — PLAN: Tess-Dispatched Action Plan Peer Review — Integration

**Context:** Tess (claude.ai session) dispatched `action-plan.md` and `tasks.md` to 4 reviewers (DeepSeek, Gemini, ChatGPT, Perplexity) plus independent Claude Opus review. Synthesis delivered via inbox as `action-plan-review-synthesis.md`.

**Context inventory:**
- `_inbox/action-plan-review-synthesis.md` — Tess synthesis (243 lines, 14 action items across 3 tiers)
- `action-plan.md` — target for structural amendments
- `tasks.md` — target for task-level amendments

**Review quality:** ChatGPT and Perplexity rated high signal. DeepSeek medium (3 of 5 gaps were stale — reviewed pre-amendment state). Gemini low (pure summary, no new findings). Claude Opus independent review caught M7 dependency graph error and Approval Contract gap.

**Tier 1 — Address before implementation (6 items, all applied):**
1. **Approval Contract as first-class task (TOP-049)** — added task with AID-* schema, Telegram UI, Discord mirror, wrapper enforcement, acceptance tests. All Phase 2+ write tasks (TOP-031, 032, 036, 037, 043) now depend on TOP-049.
2. **Ops Metrics v1 harness (TOP-050)** — added M0.4 task with structured run log, weekly report generator. Gate evaluations require "metrics evidence bundle."
3. **Gate log standardization** — defined `_openclaw/state/gates/` location, format with failure-requires-hypothesis rule.
4. **Sudoers binary scoping** — TOP-019 tightened: absolute paths, `cat`/`cp` constrained to iCloud Drive paths via wrapper.
5. **M7 dependency graph correction** — feed-intel now branches from M1 (not M4) in ASCII diagram.
6. **Cron guardrails infrastructure (TOP-051)** — added M0.5 task with single-flight lock, max wall time, missed-run behavior, jitter.

**Tier 2 — Incorporated into plan (8 items, all applied):**
7. TOP-009 scoping — already fixed (Crumb peer review A1), confirmed still correct.
8. TOP-012 dependency direction — reversed to depend on TOP-001/002, concurrent with jobs.
9. Feed-intel parallel verification (TOP-052) — 3-day parity run between monitoring and ownership.
10. Max parallel streams cap — governance rule added: ≤2 service families in Phase 1+.
11. Email denylist — TOP-037 domain denylist for government, financial, non-contact-history domains.
12. Drive risk review — TOP-042 requires written risk review before scope upgrade.
13. Anticipatory session schema — TOP-047 updated with §14 schema reference.
14. Runbook retargeting — TOP-001 pre-step: review v2026.2.25 changelog delta.

**Tier 3 — Noted for implementation (6 items, captured in synthesis doc):**
- Voice heartbeat frequency calibration (Week 1 tuning item)
- Discord canary in Phase 0 (move health check earlier)
- Wrapper enforcement before send capability (negative tests)
- iMessage contacts constraint (allowlist must match Contacts records — applied to TOP-043)
- macOS prerequisite restatement in M2.2 prose
- `tess-state.md` gate level field

**Task impact:** 48 → 52 tasks. 9 → 10 high-risk tasks (TOP-049 added). M0 expanded with M0.4/M0.5. M7 expanded with TOP-052 parallel verification step.

**Filed synthesis to:** `reviews/2026-02-27-action-plan-tess-synthesis.md`

**Decision:** All Tier 1 and Tier 2 findings applied. Tier 3 captured for implementation phase. Plan is fully amended from both review rounds.

### Session-End Compound

**Conventions confirmed:**
- Two-round peer review (Crumb-dispatched + Tess-dispatched) provides complementary coverage. Crumb's panel caught operational consistency issues (M1 calendar dependency, critical path gap). Tess's panel + independent review caught structural gaps (Approval Contract as missing deliverable, Ops Metrics harness, dependency graph error). Neither round alone would have caught everything.
- Claude Opus independent review (as part of Tess synthesis) was the highest-signal contributor this round — caught M7 dependency graph error and Approval Contract gap that 4 external reviewers missed. Independent synthesis-author review adds unique value.

**Patterns observed:**
- Gemini rated low in both Crumb and Tess rounds on the action plan, but was the highest-signal reviewer on specs (Gmail filter race condition, approval mirror message ID). Gemini's strength is precise technical analysis of integration specs, not plan-level review. Consistent with prior calibration.
- ChatGPT produced the strongest Tier 1 findings in the Tess round (Approval Contract, measurement harness, sudoers scoping). Its "protocol layer" framing was the most architecturally coherent reviewer perspective.

**Routing:** No new patterns for `_system/docs/solutions/` — the two-round peer review pattern is already captured in the SPECIFY session compound. Reviewer calibration observations accumulate but don't yet warrant a dedicated calibration doc (threshold: 5+ data points per reviewer).

### Phase Transition: PLAN → TASK
- Date: 2026-02-27
- PLAN phase outputs: action-plan.md (9 milestones, 52 tasks), tasks.md (atomic task list with dependencies and acceptance criteria), action-plan-summary.md (compressed reference), 4 review notes (2 Crumb-dispatched, 2 Tess-dispatched synthesis), frontier ideas integration, feed-intel ownership integration, cross-spec consistency propagation
- Compound: All insights captured in session-end compounds during PLAN phase (two-round peer review pattern, reviewer calibration, cross-spec consistency checks, cost ceiling propagation). No new compoundable insights at this boundary.
- Context usage before checkpoint: fresh session (~5%)
- Action taken: none needed (fresh session)
- Key artifacts for TASK phase: tasks.md, action-plan-summary.md, specification-summary.md, tess-chief-of-staff-spec.md (targeted sections per task)

## 2026-02-27 — TASK: M0 Implementation Begin

**Context inventory:**
- `tasks.md` — full task list (52 tasks, loaded)
- `action-plan-summary.md` — milestone map and dependencies (loaded)
- `project-state.yaml` — project state (loaded)

**M0 scope (7 tasks):** TOP-001 through TOP-005, TOP-050, TOP-051. Infrastructure foundation — OpenClaw upgrade, kill-switch, dead man's switch, operational state file, macOS verification, ops metrics harness, cron guardrails.

**Architecture decision:** All Tess operational scripts live in `_openclaw/scripts/` (per spec §9). State files in `_openclaw/state/`. Metrics log in `_openclaw/logs/ops-metrics.jsonl` (JSONL format). Lock files in `/tmp/openclaw-cron-locks/` (ephemeral — reboot clears stale locks naturally). Directories created with `crumbvault` group write access for cross-user operation.

### TOP-005 — Verify macOS version (DONE)
- `sw_vers`: macOS 26.3 (BuildVersion 25D125)
- Exceeds 26.2 minimum. CVE-2025-43530 (TCC bypass via AppleScript/VoiceOver) patched in 26.2.
- Result: **PASS**

### TOP-001 — OpenClaw upgrade (DONE — completed earlier today)
- Operator confirmed upgrade was executed today (2026-02-27) using the peer-reviewed runbook.
- Gateway verified alive: `curl http://127.0.0.1:18789/` returns HTTP 200. Process `openclaw-gateway` (PID 315) running as openclaw user.
- Note: `lsof -nP -iTCP:18789` shows nothing without sudo — openclaw-owned sockets are invisible to tess user. Use `nc -z` or `curl` instead (saved to MEMORY.md).
- Marking complete per operator attestation. Post-upgrade verification items (V1–V10 from runbook) should be confirmed during first interactive Tess session.
- Result: **PASS (operator-attested, gateway verified alive)**

### TOP-002 — Global kill-switch (DONE)
- Enforcement implemented in `_openclaw/scripts/cron-lib.sh` → `check_kill_switch()`.
- Checks for `/Users/openclaw/.openclaw/maintenance` file. If present, exits 0 immediately (HEARTBEAT_OK equivalent for heartbeats, silent skip for cron jobs).
- Tested: `check_kill_switch` returns normally when file absent (correct). File creation/removal requires `sudo -u openclaw` — operator action by design (emergency stop).
- All cron jobs will use `cron_init()` which calls `check_kill_switch` first.
- Result: **PASS**

### TOP-003 — Dead Man's Switch (DONE — script ready, Uptime Robot setup pending)
- Push-model health ping script created: `_openclaw/scripts/health-ping.sh`.
- Checks two signals: (1) gateway HTTP 200 on loopback:18789, (2) last cron job ran within 4 hours (reads ops-metrics.jsonl). Both must pass for external ping to fire.
- Designed for 15-minute cron schedule. External monitor alerts after 2 hours of no pings.
- **Deployment pending:** Operator needs to create Uptime Robot account, add heartbeat monitor, set `TESS_HEALTH_PING_URL` env var. Cron entry added during M1 deployment.
- Result: **PASS (script complete, external service setup deferred to deployment)**

### TOP-004 — Operational state file (DONE)
- Created `_openclaw/state/tess-state.md` with schema: system status table, active cron jobs, last heartbeat timestamps, critical system status, gate status.
- Initial values populated from verified data (OpenClaw v2026.2.25, macOS 26.3, LaunchDaemon supervisor).
- Mechanic heartbeat (TOP-007) will update this file on each cycle once configured.
- Result: **PASS**

### TOP-050 — Ops Metrics v1 harness (DONE)
- Metrics logging integrated into `_openclaw/scripts/cron-lib.sh`. Every `cron_finish()` call appends JSONL to `_openclaw/logs/ops-metrics.jsonl`.
- Fields: job_id, start_time, end_time, status, tokens_in, tokens_out, tool_calls, exit_code, alert_emitted, cost_estimate, wall_time_seconds.
- Weekly report generator: `_openclaw/scripts/weekly-ops-report.sh` — computes daily cost, alert count, job success rates, total wall time. Output is Markdown, suitable for archiving.
- Integration tested: simulated job → JSONL entry verified → cleanup.
- Result: **PASS**

### TOP-051 — Cron guardrails infrastructure (DONE)
- Shared library: `_openclaw/scripts/cron-lib.sh` — all cron jobs source this.
- **Single-flight lock:** `mkdir`-based atomic lock with PID-based stale lock recovery. Tested: live PID → lock held (correct), dead PID → stale lock recovered (correct).
- **Wall time:** Background watchdog process sends SIGTERM after max_seconds. Cleanup trap handles orderly shutdown.
- **Missed-run:** Compares last-run epoch file against max catch-up hours. Skips if too old.
- **Jitter:** Random delay up to N seconds for non-time-sensitive jobs.
- **Kill-switch:** Integrated check at cron_init entry point.
- Syntax-checked with `bash -n`. Integration test passed: init → work → set metrics → finish → JSONL verified.
- Result: **PASS**

### TOP-006 — Voice agent heartbeat config (DONE)
- Heartbeat interval: 30 min, configured via deployment script (`m1-deploy.sh` Step 3)
- HEARTBEAT.md content: 5 checks (inbox age, outbox relay, cron health, vault mirror, feed digest)
- Kill-switch check at entry (first HEARTBEAT.md instruction)
- Cheap-checks-first: all checks use shell commands via exec tool, LLM only processes output
- **Deployment:** Run `m1-deploy.sh` as openclaw user
- Result: **PASS (built, deployment pending)**

### TOP-007 — Mechanic agent heartbeat config (DONE)
- Heartbeat interval: 60 min, 24/7, configured via deployment script
- HEARTBEAT.md content: 6 checks (Ollama health, gateway health, disk usage, stale locks, gateway errors, LaunchDaemon status)
- Kill-switch check at entry. Local model (qwen3-coder) — $0 cost per tick
- Result: **PASS (built, deployment pending)**

### TOP-008 — HEARTBEAT.md entries (DONE)
- Voice: 5 entries (under 10-entry cap). Staged at `_openclaw/staging/m1/voice-HEARTBEAT.md`
- Mechanic: 6 entries (under 10-entry cap). Staged at `_openclaw/staging/m1/mechanic-HEARTBEAT.md`
- Both include kill-switch check as first item, use cheap-checks-first pattern
- Result: **PASS**

### TOP-009 — Morning briefing cron job (DONE)
- Prompt staged at `_openclaw/staging/m1/morning-briefing-prompt.md`
- Schedule: `0 7 * * *` (7 AM ET daily). Agent: voice (Haiku 4.5), isolated session
- Token budget: 15k. Content: overnight activity, pipeline health, project status, cron results, inbox/outbox. NO calendar/reminders (M3 additions).
- Result: **PASS (built, deployment pending)**

### TOP-010 — Nightly vault health check (DONE)
- Prompt staged at `_openclaw/staging/m1/vault-health-prompt.md`
- Schedule: `0 2 * * *` (2 AM ET nightly). Agent: mechanic (qwen3-coder), $0 cost
- Token budget: 10k. Runs vault-check.sh, reports errors, writes warnings to notes for next briefing.
- Result: **PASS (built, deployment pending)**

### TOP-011 — Hourly pipeline monitoring (DONE)
- Prompt staged at `_openclaw/staging/m1/pipeline-monitoring-prompt.md`
- Schedule: every 1h. Agent: mechanic, $0 cost
- Token budget: 5k. Silent when healthy — alerts only on failures.
- Result: **PASS (built, deployment pending)**

### TOP-012 — Per-job token budgets (DONE)
- Budgets enforced at prompt level (15k briefing, 10k vault, 5k pipeline) + `--timeout-seconds` on cron add
- Weekly cost report via `_openclaw/scripts/weekly-ops-report.sh`
- Result: **PASS**

### TOP-013 — Alerting rules (DONE)
- Alerting embedded in cron prompts and HEARTBEAT.md. Kill-switch → maintenance suppresses all. Heartbeat interval = natural dedup window.
- Result: **PASS**

### M1 Deployment Package
- `_openclaw/scripts/m1-deploy.sh` — interactive deployment script
- `_openclaw/staging/m1/` — 5 content files + 1 LaunchAgent plist
- CLI flags verified against `openclaw cron add --help` (v2026.2.25)
- **Deploy:** `sudo -u openclaw bash /Users/tess/crumb-vault/_openclaw/scripts/m1-deploy.sh`

### TOP-014 — Week 1 gate evaluation
- Pending deployment. Runs 5 days after M1 goes live.
- Gate criteria: briefing utility ≥3/5, alert false-positive <25%, cost ≤$3/day, stability ≥80% self-heal, ≥1 prompt revision
- Evidence: `_openclaw/logs/ops-metrics.jsonl` + weekly report

### Code Review — milestone M0+M1
- Scope: 11 files, +1022 lines (cron-lib.sh, health-ping.sh, weekly-ops-report.sh, m1-deploy.sh, plist, heartbeats, prompts)
- Panel: Claude Opus 4.6, Codex GPT-5.3-Codex
- Codex tools: bash -n (4 scripts pass), plutil -lint (pass), rg (cross-ref), nl -ba (11 files) — 27 commands total
- Findings: 3 critical, 10 significant, 10 minor, 6 strengths
- Consensus: 3 findings flagged by both reviewers (alert count bug, fail-open health check, no test suite)
- Details:
  - [CDX-F4] SIGNIFICANT: `vault-health-prompt.md:12` — wrong vault-check.sh path (runtime failure)
  - [ANT-F10/CDX-F3] MINOR: `weekly-ops-report.sh:68` — alert count uses wc -l on non-compact jq output
  - [ANT-F2] CRITICAL: `cron-lib.sh:150-159` — JSON injection via printf in metrics logging
  - [CDX-F1] CRITICAL: `health-ping.plist` — TESS_HEALTH_PING_URL missing from plist env vars
  - [ANT-F13/CDX-F2] SIGNIFICANT: `health-ping.sh:75-83` — fail-open when metrics log missing/corrupt
  - [CDX-F6] SIGNIFICANT: `cron-lib.sh:210` — unvalidated job-id used in rm -rf paths
  - [ANT-F6/CDX-F5] SIGNIFICANT: `m1-deploy.sh:133-141` — jq config update may silently no-op
- Action: 2 must-fix, 7 should-fix, 7 defer — all must-fix and should-fix items applied
- Review note: Projects/tess-operations/reviews/2026-02-26-code-review-milestone.md

## 2026-02-26 — Session End (continued session)

**Context inventory:** project-state.yaml, run-log.md, tasks.md, action-plan-summary.md, cron-lib.sh, health-ping.sh, m1-deploy.sh, health-ping.plist, code-review-config.md, code-review-dispatch agent, session-end-protocol.md (11 docs — extended tier, justified by M0+M1 implementation + code review in single session)

**Work completed:**
1. PLAN → TASK phase transition (context checkpoint, run-log, progress-log, project-state)
2. M0 infrastructure — 7 tasks done (TOP-001-005, TOP-050-051): cron-lib.sh, health-ping.sh, weekly-ops-report.sh, tess-state.md
3. M1 chief-of-staff MVP — 8 tasks built (TOP-006-013): heartbeat configs, cron job prompts, m1-deploy.sh, LaunchAgent plist
4. Code review — two-reviewer panel (Opus + Codex). 3 critical, 10 significant findings. All must-fix (2) and should-fix (7) items applied.
5. 7 commits pushed: phase transition, M0, M1, table formatting, code review note, code review fixes

**Compound:**
- **Code review value signal:** First full code review with Opus+Codex panel on shell scripts. Both reviewers passed syntax validation. Consensus findings (3) were highest signal — the alert count bug and fail-open health check would have been real runtime issues. Codex's tool-grounded approach (27 commands) found the vault-check.sh path bug that reasoning alone didn't catch. Opus found the JSON injection pattern that Codex missed. Complementary value confirmed.
- **Pattern: jq for JSON construction in bash** — `printf` with `%s` format specifiers is a JSON injection vector. `jq -n -c --arg` is the safe idiomatic alternative. Applies to all future shell scripts that produce structured output. Low-confidence (single occurrence) but mechanically sound — defer pattern capture until second occurrence.
- **Pattern: fail-open monitoring** — health checks that return success on missing/corrupt data hide outages. Grace period with time-based escalation is the right fix: pass during bootstrap, fail after steady-state threshold. Applicable to any monitoring script.

**Promote:** no

## 2026-02-27 — TASK: Heartbeat Cost Optimization Applied

**Context inventory:**
- `_inbox/heartbeat-cost-optimization-recommendation.md` — Tess recommendation (165 lines)
- `_openclaw/staging/m1/voice-HEARTBEAT.md` — voice heartbeat staged file
- `_openclaw/staging/m1/mechanic-HEARTBEAT.md` — mechanic heartbeat staged file
- `_openclaw/scripts/m1-deploy.sh` — deployment script
- `tasks.md` — task list
- `design/tess-chief-of-staff-spec.md` §11 — cost table
- OpenClaw docs (docs.openclaw.ai/automation/cron-jobs) — CLI flag verification

**Recommendation:** Eliminate voice heartbeat as the single largest cost driver ($15–45/month, 50–65% of chief-of-staff budget). Redistribute workload to existing zero-cost infrastructure: mechanic heartbeat (binary checks) + local-model cron job (content-aware checks).

**Rationale:** Native `heartbeat.model` override is broken (OpenClaw issues #9556, #14279, #22133 — the third causes model bleed corrupting the main session). Workaround: `openclaw cron add` with `--session isolated --model` correctly respects overrides. Voice heartbeat workload is entirely suitable for local inference (qwen3-coder:30b).

### CLI Flag Verification

Verified against `openclaw cron add` documentation (docs.openclaw.ai/automation/cron-jobs):
- `--model` ✓ — provider/model override for isolated jobs
- `--channel` ✓ — target channel (telegram, discord, etc.)
- `--session isolated` ✓ — isolated execution environment
- `--timeout-seconds` ✓ — already in deploy script
- `--stagger` ✓ — already in deploy script
- `--best-effort-deliver` ✓ — already in deploy script
- `--announce` ✓ — already in deploy script
- `--no-deliver` ✗ — **not a real flag**, removed from pipeline-monitor in m1-deploy.sh

### Changes Applied

1. **Voice HEARTBEAT.md** — replaced with disabled stub explaining migration
2. **Mechanic HEARTBEAT.md** — expanded from 6 to 9 checks: added inbox age (>4h), cron job health, vault mirror freshness (migrated from voice)
3. **Awareness-check prompt** — new file `_openclaw/staging/m1/awareness-check-prompt.md` for outbox relay + digest flagging
4. **m1-deploy.sh** — voice heartbeat set to "0" (disabled), awareness-check cron job added (`*/30 7-23 * * * ET`, `--session isolated --model ollama/tess-mechanic:30b --channel telegram`), `--no-deliver` flag removed from pipeline-monitor, staging file prereq updated
5. **tasks.md** — TOP-006 amended (disabled), TOP-007 amended (expanded), TOP-008 amended (redistributed), TOP-053 added (awareness-check cron). Count: 52 → 53
6. **Chief-of-staff spec §11** — cost table updated: voice heartbeat $15-45 → $0, awareness check row added ($0), chief-of-staff total $30-70 → $7-25, full stack $75-115 → $52-70
7. **Recommendation filed** to `design/heartbeat-cost-optimization-recommendation.md`

**Frequency decision:** Awareness check runs every 30 minutes during waking hours (7–23 ET) per operator instruction. Zero cost means no reason to reduce frequency; outbox relay responsiveness has real value during active work. Mechanic stays at 60 minutes — that workload doesn't benefit from higher frequency.

**Pre-existing fix:** `--no-deliver` flag on pipeline-monitor was not a real OpenClaw CLI flag. Removed. The prompt already handles silence-when-healthy behavior ("alert on failure only"), so no delivery flag is needed.

**Cost impact:** Chief-of-staff drops from $30–70/month to $7–25/month (~55–65% reduction). Full stack from $75–115 to $52–70. Comfortable margin under $100 hard cap and $120 rollback trigger.

**Next action:** Deploy M1 (run m1-deploy.sh as openclaw). Syntax check passed.

## 2026-02-27 — M1 Deployment

**Context inventory:**
- `_openclaw/scripts/m1-deploy.sh` — deployment script
- `_openclaw/staging/m1/` — 6 staging files + 1 plist
- `_openclaw/scripts/health-ping.sh` — dead man's switch script
- `project-state.yaml`, `tasks.md`, run-log

### Deployment Execution

Ran `m1-deploy.sh` as openclaw user. Results:

**Step 0 — Prerequisites:** OpenClaw v2026.2.25 ✓, gateway alive ✓, staging files present ✓

**Step 1 — Agent discovery:** voice (Haiku 4.5) and mechanic (qwen3-coder) found in openclaw.json. Mechanic had pre-existing heartbeat at 30m (overridden to 60m in Step 3).

**Step 2 — HEARTBEAT.md:** Both deployed to agent workspace directories ✓

**Step 3 — Heartbeat intervals:** Voice set to "0" (disabled) ✓, mechanic set to 60m ✓. Post-verification confirmed in config.

**Step 4 — Cron jobs:**
- `morning-briefing` (voice, `0 7 * * *` ET) — registered ✓
- `vault-health` (mechanic, `0 2 * * *` ET) — registered ✓
- `pipeline-monitor` (mechanic, `0 * * * *` ET) — **FAILED first attempt** (`--stagger` incompatible with `--every`). Fix: changed to `--cron "0 * * * *"` with `--tz`. Re-registered manually ✓
- `awareness-check` (mechanic, `*/30 7-23 * * *` ET, ollama/tess-mechanic:30b) — registered ✓

**Step 5 — Health ping:** Installed separately as tess-user LaunchAgent. Plist copied, xattr stripped, `plutil -lint` passed, bootstrapped to `gui/501`. Test fire confirmed: both signals healthy, ping correctly skipped (no Uptime Robot URL configured yet).

**Step 7 — Gateway restart:** `openclaw gateway restart` failed (service runs as LaunchDaemon in system/ domain, not LaunchAgent). Used `env -u TERMINFO sudo launchctl kickstart -k system/ai.openclaw.gateway`. Gateway confirmed alive after restart.

### Issues Found During Deploy

1. **`--stagger` requires `--cron`:** Deploy script used `--every "1h"` for pipeline-monitor, but `--stagger` is only valid with `--cron`. Fixed in deploy script: `--every "1h"` → `--cron "0 * * * *"` + `--tz "America/New_York"`.

2. **`sudo -u openclaw` HOME not reset:** Manual `openclaw cron add` as openclaw hit `EACCES: permission denied, mkdir '/Users/tess/.openclaw/identity'`. Fixed with `sudo -u openclaw env HOME=/Users/openclaw ...`. Known pattern (documented in memory).

3. **`openclaw gateway restart` doesn't see LaunchDaemon:** CLI looks in gui/$UID domain only. Gateway runs in system/ domain. Used `launchctl kickstart -k system/ai.openclaw.gateway` instead. TERMINFO sudoers restriction required `env -u TERMINFO` prefix.

### Deployment Verification

All 4 cron jobs visible in `openclaw cron list`:
| Job | Schedule | Next Run | Agent |
|-----|----------|----------|-------|
| awareness-check | `*/30 7-23 * * *` ET | ~30m | mechanic |
| vault-health | `0 2 * * *` ET | ~4h | mechanic |
| morning-briefing | `0 7 * * *` ET | ~9h | voice |
| pipeline-monitor | `0 * * * *` ET | ~30m | mechanic |

Heartbeat config confirmed: voice=0, mechanic=60m. Both HEARTBEAT.md files present. Health-ping LaunchAgent loaded and test-fired.

### Pending

- **Uptime Robot setup:** Create heartbeat monitor, set `TESS_HEALTH_PING_URL` in plist, `launchctl kickstart` to reload. Until then, health-ping runs but skips the external ping.
- **TOP-014 gate evaluation:** 5-day observation period starts now. Gate criteria: briefing utility ≥3/5, alert false-positive <25%, cost ≤$3/day, stability ≥80% self-heal, ≥1 prompt revision.

**Next action:** Monitor M1 for 3 days (TOP-014). First morning briefing fires tomorrow 7 AM ET. First vault-health tonight 2 AM ET. Pipeline-monitor and awareness-check within the hour.

## 2026-02-27 — Session End

**Context inventory:** project-state.yaml, run-log.md, tasks.md, action-plan.md, action-plan-summary.md, tess-chief-of-staff-spec.md, m1-deploy.sh, health-ping.sh, ai.openclaw.health-ping.plist, claude-ai-context.md (10 docs — design ceiling, justified by deployment + cross-doc gate duration update)

**Work completed:**
1. M1 deployment — ran m1-deploy.sh as openclaw. 4 cron jobs registered (morning-briefing, vault-health, pipeline-monitor, awareness-check). Voice heartbeat disabled, mechanic at 60m. Gateway restarted.
2. Health-ping LaunchAgent installed — plist deployed to tess user, bootstrapped, test-fired (correctly skips ping with no Uptime Robot URL).
3. Deploy script fix — `--every "1h"` → `--cron "0 * * * *"` for pipeline-monitor (`--stagger` requires `--cron`).
4. Gate duration reduction — all gate evaluations shortened from 5 days to 3 days across spec, action plan, tasks, and summary. Proportional threshold adjustments (briefing ≥3/5→≥2/3, failure extension 3→2 days).
5. Context file fix — task count 15/52 → 16/53.

**Compound:**
- **Pattern: OpenClaw CLI domain mismatch.** `openclaw gateway restart` and `openclaw cron status` look in the user's gui/$UID LaunchAgent domain. When the gateway runs as a system LaunchDaemon, these commands report "not loaded." All gateway management must go through `launchctl` targeting `system/ai.openclaw.gateway`. This affects any future restart/status automation. Promote: update OpenClaw operational notes in memory.
- **Pattern: `--stagger` requires `--cron`.** OpenClaw's `--every` interval scheduling doesn't support `--stagger` (jitter). Only `--cron` expression scheduling does. Discovered at deploy time. Low-impact (single occurrence) — defer pattern capture.
- **vault-check §10 format gap.** `active_task` validation greps for YAML `id: <task-id>` but tess-operations uses markdown table format. Workaround: set active_task to null. Not worth fixing immediately — only one project uses table format.

**Promote:** no

## 2026-02-27 — Post-Deploy Validation

**Context inventory:** run-log.md, tasks.md, m1-deploy.sh, cron runs output, openclaw cron help (5 docs)

### Cron Job Validation Results

Monitored first cron cycle after M1 deployment.

**pipeline-monitor:** ✓ ok. Ran on schedule, returned "HEARTBEAT_OK", `delivered: false` (correct — silent when healthy). Local model, $0 cost.

**awareness-check:** Three iterations to get delivery routing right:
1. **v1 (mechanic + `--channel telegram`):** `status: error`, "Message failed". Mechanic agent has no Telegram channel binding.
2. **v2 (voice + `--model ollama/tess-mechanic:30b`):** `status: error`. Model override ignored at runtime — used Haiku 4.5 instead of local model (#9556/#14279 bug family: `--model` in isolated sessions is broken). Cost hit + agent errored on execution.
3. **v3 (mechanic + `--to <chatId>`):** `status: ok`, `delivered: true`. Direct delivery bypasses agent channel bindings. Correct model (tess-mechanic:30b via ollama). **However:** Telegram receipt unconfirmed despite `delivered: true` status.

### Known Issues (gate evaluation items)

1. **Exec tool access in isolated sessions:** awareness-check reports "No actionable items found" despite 60+ stale files in outbox. The local model is generating plausible responses without executing shell commands. Evidence: 62-81k input tokens for a ~200 token prompt (full agent context loaded), test file with known stale timestamp not detected.

2. **Telegram delivery via `--to`:** OpenClaw reports `delivered: true` but operator did not receive messages. Chat ID may be incorrect (phone number vs Telegram user ID), or delivery is silently failing.

3. **Token overhead in isolated sessions:** 60-80k input tokens per run. "Isolated" sessions still load the mechanic agent's full context (system prompt, HEARTBEAT.md, workspace). Cost implication if awareness-check ever falls back to Haiku.

### Design Insight

The awareness-check should be restructured as a **bash script** (like health-ping.sh) rather than an LLM agent conversation. Shell commands (`find`, `ls -t`) run directly, with the LLM invoked only when there's content to summarize. This is the cheap-checks-first pattern taken to its logical conclusion — the LLM shouldn't be running `find` commands.

### OpenClaw CLI Learnings

- `cron rm <name>` silently fails (`"removed": false`) — must use UUID: `cron rm <uuid>`
- `cron run <name>` also fails — must use UUID
- `cron run` default timeout is 30s (`--timeout 30000`), local model jobs take 50-70s — use `--timeout 120000`
- `cron logs` doesn't exist — use `cron runs --id <uuid>`

**Next action:** Morning briefing fires at 7 AM ET — first real operator-facing test. Vault-health at 2 AM. Continue 3-day gate eval (TOP-014). Awareness-check bash script restructure is a prompt revision item within the gate period.

## 2026-02-27 — Session End (deployment + validation session)

**Context inventory:** project-state.yaml, run-log.md, tasks.md, action-plan.md, action-plan-summary.md, tess-chief-of-staff-spec.md, m1-deploy.sh, health-ping.sh, ai.openclaw.health-ping.plist, claude-ai-context.md, openclaw cron help/runs output (11 docs — extended tier, justified by deployment + live debugging + cross-doc gate duration update)

**Work completed:**
1. M1 deployment — 4 cron jobs registered, heartbeats configured, gateway restarted, health-ping LaunchAgent installed
2. Deploy script fix — `--every` → `--cron` for pipeline-monitor (`--stagger` requires `--cron`)
3. Gate duration reduction — 5 days → 3 days across all governing docs (spec, action plan, tasks, summary)
4. Context file fix — task count 15/52 → 16/53
5. Awareness-check delivery debugging — three iterations: mechanic+channel (no binding), voice+model override (broken per #9556), mechanic+`--to` (delivery reports ok, receipt unconfirmed)
6. Post-deploy validation — pipeline-monitor confirmed working, awareness-check has known issues (exec tool access, delivery receipt, token overhead)

**Compound:**
- **Pattern: OpenClaw `--model` override broken in isolated sessions.** Same bug family as `heartbeat.model` (#9556/#14279). Payload stores override correctly, runtime ignores it. Confirmed via cron runs data: registered with `ollama/tess-mechanic:30b`, ran with `claude-haiku-4-5`. Affects all cron jobs that try to override the agent's default model. Workaround: don't override — use the agent whose default model you want, and use `--to` for delivery routing. Promoted to memory.
- **Pattern: OpenClaw cron CLI uses UUIDs, not names.** `cron rm`, `cron run`, `cron runs` all require the job UUID as positional arg. Name-based lookup silently fails (`"removed": false`). Promoted to memory.
- **Pattern: cheap-checks-first as architecture, not just optimization.** The awareness-check failure crystallizes this: LLM agents shouldn't run `find` commands. Bash scripts run cheap checks directly; LLM is invoked only when there's content requiring judgment. health-ping.sh already follows this pattern. awareness-check should be restructured the same way. This generalizes to all future monitoring jobs.

**Promote:** no

## 2026-02-27 — Awareness-Check Delivery Fix (TOP-014 Gate Eval Day 2)

### Context Inventory
- run-log.md, project-state.yaml
- health-ping.sh, cron-lib.sh (reference patterns)
- awareness-check-prompt.md (original prompt)
- ai.openclaw.health-ping.plist (reference plist)
- OpenClaw gateway logs (stdout + stderr)
- OpenClaw-Heartbeat-Cron-Issues-fixes.md (inbox research doc, ~47KB)

### Diagnostic Chain

**Symptom:** Awareness-check cron job reports `delivered: true`, `deliveryStatus: "delivered"` but nothing reaches Telegram. Morning briefing (voice agent) delivers fine.

**Attempted fixes (each insufficient alone):**
1. Added mechanic to `bindings` array in `openclaw.json` — necessary but not sufficient
2. Recreated cron job with explicit `--channel telegram --to 7754252365` — `delivery.to` stored but ignored

**Root cause (confirmed via gateway logs):**
- `[cron:4afba591…] No delivery target resolved for channel "telegram". Set delivery.to.` — gateway explicitly says it can't resolve the target
- `delivery.to` regression in v2026.2.25: value stored in cron payload but runtime ignores it
- `bestEffort: true` masks the failure — `deliveryStatus: "delivered"` is misleading
- DM pairings are in-memory only (not in `openclaw.json`, `telegram-pairing.json`, or SQLite) — lost on gateway restart
- Voice agent works because it has an active DM pairing; mechanic never had one

**Additional findings:**
- Sandbox restriction: `[tools] read failed: Path escapes sandbox root (~/.openclaw/workspace-mechanic)` — mechanic agent can't read vault paths (repeated dozens of times in stderr)
- vault-health cron job timed out: `[diagnostic] lane task error: durationMs=600005 error="FailoverError: LLM request timed out."` — 600s limit exceeded
- Old cron job UUID (4afba591) logged the target resolution error; new job (3ca7bf8e) logged nothing — different code path, same silent failure
- `@tdusk42_bot` is the gateway's Telegram bot username
- `delivery.to` regression tracked as #27898 in the OpenClaw issues reference doc

### Solution: Bash Script Migration

**Decision:** Replace OpenClaw cron job with standalone bash script. Three options evaluated:
1. Upgrade to v2026.2.26 (fixes delivery.to) — correct fix, wrong timing (mid-gate-eval)
2. Route through voice agent — `--model` override broken (#9556), would use Haiku (costs money)
3. **Bash script + direct Telegram Bot API** — zero OpenClaw dependency, proven pattern (health-ping.sh)

Option 3 selected. Already identified in previous session as the long-term architecture.

**Files created:**
- `_openclaw/scripts/awareness-check.sh` — 160 lines, sources cron-lib.sh, two checks (outbox relay + feed freshness), direct curl to Telegram Bot API, HTML parse mode, waking hours gate (07–23)
- `_openclaw/staging/m1/ai.openclaw.awareness-check.plist` — LaunchAgent, 1800s interval, RunAtLoad, env var for bot token

**Validation:** Script ran successfully — 10 stale outbox files relayed, Telegram message received with HTTP 200. First successful awareness-check delivery.

### Compound

- **Pattern: `delivery.to` + `bestEffort` + misleading status = triple-silent-failure.** Three independent mechanisms conspire to hide delivery failures: broken parameter, error suppression, and incorrect status reporting. Diagnosis required gateway log analysis to cut through the abstraction. Promoted to memory.
- **Pattern: bash for checks, curl for delivery, OpenClaw for thinking.** Lightweight operational checks don't need an LLM. Reserve OpenClaw cron for jobs requiring model inference. Direct Telegram Bot API via curl bypasses bindings, pairings, and delivery.to resolution. Promoted to memory.
- **Pattern: DM pairings are volatile state.** In-memory only, lost on gateway restart. Any delivery path that depends on pairings is fragile. Direct API delivery (curl) or explicit target configuration is more resilient.

**Promote:** triple-silent-failure pattern, bash-for-checks architecture

## 2026-02-27 — Awareness-Check Bugfixes + Vault-Health Bash Migration

### Context Inventory
- run-log.md, awareness-check.sh, ai.openclaw.awareness-check.plist (installed)
- cron-lib.sh, health-ping.sh, ai.openclaw.health-ping.plist (reference patterns)
- vault-health-prompt.md, vault-check.sh output (migration source)

### Work Completed

**Awareness-check bugfixes (2):**
1. **Octal hour bug** — `date +%H` returns `08`/`09` with leading zero, bash `[[` interprets as octal → "value too great for base" error at 08:xx and 09:xx daily. Fix: `date +%-H` (no zero-padding). LaunchAgent reloaded.
2. **Relayed dir permissions** — `_openclaw/outbox/relayed/` created by openclaw user with `755` (no group write). Tess user's `mv` failed silently (`|| true`). Fix: recreated dir as tess with `775`, added `-m 775` to `mkdir -p` in script. Verified: 10 files moved to relayed on next run.

**Vault-health bash migration (TOP-010):**
3. **Root cause confirmed** — OpenClaw cron job timed out at 600s (10 min ceiling). Mechanic agent in sandboxed isolated session can't read vault paths (`Path escapes sandbox root`). Same root cause as awareness-check.
4. **Built `_openclaw/scripts/vault-health.sh`** — Three checks, all pure bash:
   - vault-check.sh: parse exit code (0=clean, 1=warn, 2=error) + extract ERROR/WARNING lines
   - git status: flag uncommitted files as warning
   - stale project-state: find files unmodified 14+ days
   - Errors → Telegram alert. Warnings → `_openclaw/state/vault-health-notes.md` (morning briefing context). Clean → silent + remove stale notes file.
5. **Built `_openclaw/staging/m1/ai.openclaw.vault-health.plist`** — CalendarInterval at 2 AM, same bot token as awareness-check.
6. **Tested manually** — 0 errors, 2 warnings, 9 uncommitted files. Correctly wrote health notes, no Telegram alert. Zero stderr.
7. **Installed LaunchAgent** — copied to `~/Library/LaunchAgents/`, stripped provenance xattr, bootstrapped. Verified loaded (exit 0).
8. **Removed OpenClaw cron job** — `cron rm 5c181f32-...` confirmed by operator.

**OpenClaw cron job cleanup verified:**
- `openclaw cron list` shows 3 jobs: pipeline-monitor (ok), vault-health (removed), morning-briefing (ok)
- Old awareness-check already gone (confirmed from previous session)

### Compound

- **Pattern: bash `date +%H` octal trap.** Leading-zero hours (08, 09) fail in arithmetic comparisons because bash treats `0N` as octal. Use `date +%-H` for arithmetic or `10#$hour` for explicit base-10. Joins the `set -e` + `return` and `set -o pipefail` + `grep` notes as a macOS bash gotcha. All three awareness-check/health scripts now use `%-H`.
- **Pattern: silent `|| true` on `mv` hides permission failures.** The relayed dir had wrong permissions but the script reported success because `mv ... || true` swallowed the error. Consider logging mv failures explicitly rather than blanket `|| true` in future scripts.
- **Observation:** Two of four M1 cron jobs (awareness-check, vault-health) have now been migrated from OpenClaw to bash+launchd. Pipeline-monitor and morning-briefing remain on OpenClaw — pipeline-monitor works (mechanic, local model, simple HEARTBEAT_OK), morning-briefing uses voice agent (Haiku, needs LLM synthesis). The bash-for-checks pattern is validated; only jobs requiring LLM inference should stay on OpenClaw.

**Promote:** no (patterns already captured in memory from previous session)

**Additional fixes (post-log):**
9. **health-ping.sh UTC parsing** — `date -j -f` without `-u` parses Z-suffixed UTC timestamps as local time, producing epoch 5h ahead (ET offset). `age` always negative → stale-job check never triggers. Fix: added `-u` flag. Operator-identified from trace data.
10. **healthchecks.io configured** — dead man's switch provider wired to health-ping.sh, verified working.

## 2026-02-27 — Session End (bugfix + migration session)

**Work completed:**
1. Awareness-check: fixed octal hour bug (`+%H` → `+%-H`), fixed relayed dir permissions (775)
2. Vault-health: built bash script + plist, tested, installed LaunchAgent, removed OpenClaw cron job
3. Health-ping: fixed UTC parsing (`date -j` → `date -u -j`)
4. healthchecks.io wired as dead man's switch provider
5. OpenClaw cron cleanup verified — awareness-check gone (previous session), vault-health removed this session

**Compound:**
- **Pattern: macOS `date -j -f` treats all timestamps as local time by default.** Even with explicit `Z` suffix in the format string, BSD `date` interprets the parsed value as local time unless `-u` is passed. This differs from GNU `date` which respects `Z` as UTC. Add to the macOS bash gotchas collection alongside octal hours, `set -e`+`return`, and `pipefail`+`grep`.
- **Convention confirmed:** Operator code review of bash scripts catches timezone/numeric edge cases that automated testing misses. The octal trap was caught by stderr logs; the UTC parsing was caught by operator trace analysis. Both would have been silent failures in production (negative age always passes threshold check, octal only fails at 08:xx/09:xx).

**Promote:** no (patterns already captured in memory from previous session)

## 2026-02-27 — Memory Search Spec Review + Deployment

**Context inventory:**
- `_inbox/tess-memory-search-spec.md` (v1) → reviewed, 7 issues identified
- `_inbox/tess-memory-search-spec-v2.md` → reviewed, 3 minor issues (broken table header, numbering gap, unverified GitHub issue numbers)
- `Projects/tess-operations/design/specification-summary.md` — updated with 5th spec entry

**Work completed:**
1. Reviewed v1 memory search spec from claude.ai session — identified 7 issues (unexplained compaction.mode, embedding provider not a hard gate, per-agent mechanic override deferred, CLI flag verification needed, memory flush + isolated sessions unclear, unsourced message volume, MEMORY.md seeding unowned)
2. Reviewed v2 — all 7 original issues resolved, found 3 minor issues (broken table header §3.6, section numbering gap §3.4 skipped, unverified GitHub issue numbers #4836/#5343)
3. Fixed table header and renumbering, moved to `design/tess-memory-search-spec.md`, removed v1 from inbox
4. Updated specification-summary.md (scope: 4→5 specs, cost envelope: +$0.17-0.55, key files list)
5. Cleared openclaw inbox — 223 X tweet files (3 naming format duplicates), 30 processed JSONs, 3 unrecognized. 16 RSS items retained.
6. Deployed memory search config via `apply-memory-search-config.sh` — memorySearch enabled, hybrid search, MMR, temporal decay, cache, memoryFlush, mechanic disabled
7. Resolved embedding provider: Anthropic NOT supported in v2026.2.25, configured OpenAI text-embedding-3-small (explicit provider required, auto-selection insufficient)
8. Seeded MEMORY.md in Tess workspace with initial curated facts
9. Verified memory search live via Telegram test — Tess retrieved "bullet summaries" from MEMORY.md
10. Updated spec: Open Question #1 resolved, status draft→active, deployed date added

**Compound:**
- **Pattern: OpenClaw `openclaw memory status` CLI is a misleading diagnostic.** Reports "Memory search disabled" even when the gateway runtime has memory search active and functional. The CLI runs as a separate process and doesn't reflect gateway runtime state. Always verify via Telegram test + runtime logs (`/tmp/openclaw/openclaw-YYYY-MM-DD.log`), not CLI status.
- **Pattern: OpenClaw auto-selection finds providers but doesn't fully activate features.** `Provider: openai (requested: auto)` showed the provider resolved, but memory search stayed disabled until explicit `provider: "openai"` + `model: "text-embedding-3-small"` were added to config. Auto-selection is diagnostic, not declarative.
- **Convention: `sudo -u openclaw` requires cwd in a world-readable directory.** The openclaw user can't access tess's directories. `sudo cd /tmp` doesn't work (shell builtin). Scripts must `cd /Users/openclaw` internally; interactive commands need `cd /tmp` first.

**Promote:** no (patterns captured in Crumb memory)

## 2026-02-28 — TOP-054: Browser Access for Tess (Design + Task Creation)

**Context inventory:**
- `tess-chief-of-staff-spec.md` §8.1 (overnight research), §10 (model config), §12 (security)
- `tess-model-architecture/design/routing-specification.md` §2.1-2.2 (agent definitions)
- `tess-model-architecture/design/production-config.md` (model assignments)
- `tess-model-architecture/design/benchmark-results.md` (mechanic tool-use benchmarks)
- `openclaw-colocation/design/upgrade-v2026-2-24.md` (LaunchDaemon migration, SSRF)
- OpenClaw docs: `docs.openclaw.ai/tools/browser`, `docs.openclaw.ai/gateway/security`

**Current state (verified via sudo):**
- `browser` block in openclaw.json: not set (all defaults)
- Playwright browser cache: empty (no binaries installed)
- `openclaw browser status`: only `chrome` profile (extension relay), not running, detectedPath points to system Chrome

**Design decisions (peer-reviewed):**
1. **Managed `openclaw` profile, not extension relay** — headless cron context, no GUI session available
2. **`headless: true`** — gateway runs as LaunchDaemon (`system/` domain), openclaw user has no GUI login
3. **`evaluateEnabled: false`** — JS eval in browsed pages is a prompt injection vector
4. **`dangerouslyAllowPrivateNetwork: false`** — hardening from default `true`; research sessions browse public web only
5. **`allowHostControl: false`** — sandboxed sessions cannot escape to host browser
6. **Voice agent only** — mechanic gets explicit `tools.deny: ["browser"]` because:
   - Mechanic model (qwen3-coder:30b) unsuited for iterative snapshot→reason→act cycles
   - Benchmark MC-4 shows degradation on large structured data reasoning (DOM snapshot pattern)
   - Mechanic fallback to Haiku would create silent cost leak
   - Spec §10 never assigns browser-dependent work to mechanic
7. **Chromium process sandbox** — try `noSandbox: false` first; flip to `true` if launch fails under LaunchDaemon
8. **50k token budget** — needs real-world validation against DOM snapshots during smoke test

**Config block (final):**
```json
{
  "browser": {
    "enabled": true,
    "defaultProfile": "openclaw",
    "evaluateEnabled": false,
    "headless": true,
    "ssrfPolicy": {
      "dangerouslyAllowPrivateNetwork": false
    }
  },
  "agents": {
    "list": [
      {
        "id": "mechanic",
        "tools": {
          "deny": ["browser"]
        }
      }
    ],
    "defaults": {
      "sandbox": {
        "browser": {
          "allowHostControl": false
        }
      }
    }
  }
}
```

**Implementation steps:**
1. Install Playwright Chromium: `cd /tmp && sudo -u openclaw env HOME=/Users/openclaw npx -y playwright install chromium`
2. Merge config block into openclaw.json (preserve existing keys)
3. Restart gateway
4. `openclaw browser status` — verify managed profile exists, Playwright detected
5. Smoke test: one-shot browser navigate → snapshot → verify content
6. Document Chromium sandbox behavior (noSandbox setting) in run-log
7. Verify CDP port allocation has no conflicts

**Task created:** TOP-054 (infrastructure, medium risk, depends on TOP-001)
**Dependency added:** TOP-046 now depends on TOP-054

**Peer review notes:**
- Initial Crumb plan had 4 gaps caught by reviewer: missing Playwright prerequisite, missing defaultProfile, SSRF default misframed, no mechanic tool deny mechanism
- Reviewer had 2 errors caught by Crumb: session isolation ≠ sandboxing (Finding 2), `headless: false` wrong for LaunchDaemon context
- LLM dynamic analysis (reviewer follow-up): confirmed mechanic model unsuited for browser; voice-only browser access aligns with existing spec architecture
- All parties converged on final config block above

**Execution:**
1. Playwright Chromium installed: Chrome for Testing 145.0.7632.6 (v1208) + Headless Shell + FFmpeg at `/Users/openclaw/Library/Caches/ms-playwright/` (macOS cache path, not `~/.cache/`)
2. Config merged via jq patch — 3 additions: `browser` block, mechanic `tools.deny`, sandbox `allowHostControl`
3. Gateway restarted via `launchctl kickstart -k`
4. `openclaw browser status` confirmed: managed `openclaw` profile active, CDP port 18800
5. Smoke tests passed via Telegram:
   - httpbin.org/html — static content read (basic browser+snapshot)
   - news.ycombinator.com — DOM parsing + structured extraction (snapshot→reason cycle)
6. Chromium process sandbox: works under LaunchDaemon without `noSandbox: true` — no issues observed

**Result:** TOP-054 complete. Tess has browser access via voice agent on Haiku. Mechanic explicitly denied. TOP-046 (overnight research sessions) unblocked on browser prerequisite.

## 2026-02-28 — SOUL.md Update: Written Deliverables Standards

**Context:** Tess's first browser-driven research assignment (X/Twitter profile analysis) produced output that violated existing principles — overclaiming ("could replace your bridge"), cheerleading ("validates the Crumb bet"), ungrounded recommendations. Analysis showed SOUL.md already carries the relevant principles but they're calibrated for conversational interaction, not async analytical output.

**Peer review:** 4/4 reviewers (GPT-5.2, Gemini 3 Pro, DeepSeek V3.2, Grok 4.1). Review note: `_system/reviews/2026-02-28-crumb-soul-analysis.md`. Key findings:
- Fix scope too narrow (3 reviewers) → broadened to "Written Deliverables"
- "Efficiency is respect" conflicts with "take your time" (3 reviewers) → qualified by context
- No validation plan (3 reviewers) → next research assignment is live test
- Source trust calibration missing (5 findings across 3 reviewers) → added provenance/quoting rules
- SOUL.md loading in isolated cron sessions unverifiable (4 reviewers) → empirically verified via one-shot cron test

**Changes applied to `_openclaw/staging/SOUL.md` → deployed to workspace:**
1. Modified "Efficiency is respect" line: qualified by context (conversation = brevity, analysis = thoroughness)
2. New "Written Deliverables" section (6 bullets, ~100 tokens): gap analysis requirement, hypothesis framing, source trust, confidence tracking, scope discipline, assumption transparency

**Verification:** Two one-shot cron tests confirmed:
- A10: SOUL.md loads in isolated cron sessions ("cargo rots on the dock" quoted)
- Post-deploy: new section active ("Treat Danny's premise as a hypothesis to test" quoted)

**A6 (validation):** Next research assignment will be evaluated against new standards.

**Compound:**
- **Pattern: SOUL.md principles need mode-specific operationalization.** General principles ("demand evidence," "no sycophancy") trigger reliably in conversational interaction (immediate feedback loop) but fail silently in async written output (no feedback loop). Bridge the gap with mode-specific sections, not by restating the same principles.
- **Pattern: One-shot cron jobs are the definitive test for agent system prompt loading.** `--at "1m" --delete-after-run --session isolated` with a distinctive-phrase probe confirms what the architecture docs claim. Telegram tests only validate the main session path.
