---
project: mission-control
type: tasks
domain: software
status: active
skill_origin: action-architect
created: 2026-03-07
updated: 2026-03-07
tags:
  - dashboard
  - web
  - kb/software-dev
topics:
  - moc-crumb-architecture
---

# Mission Control Dashboard — Tasks

## Phase 0: Design (no code)

### Milestone 0a: Aesthetic Exploration

| ID | Description | State | Depends On | Risk | Acceptance Criteria |
|----|-------------|-------|------------|------|---------------------|
| MC-001 | Design workspace setup + aesthetic brief | done | — | low | Design workspace directory exists at `Projects/mission-control/design/mockups/`. Aesthetic brief document produced summarizing Observatory mode principles, Library vs Observatory tension, and dark/light/hybrid exploration scope. Design Advisor + Web Design Preference overlays consulted. |
| MC-002 | Three aesthetic direction candidates for Ops page | done | MC-001 | medium | One HTML/CSS mockup file exists showing the Ops page at realistic data density with ET Book / Source Serif typography and representative widget layouts. CSS custom properties define color palette — dark, light, and hybrid variants toggled by swapping ~12 CSS variable values (layout and widgets identical across variants). All three variants render correctly in browser. [AP-9] |
| MC-003 | Aesthetic direction decision | done | MC-002 | medium | Operator has reviewed all three candidates. Decision documented in design brief: selected direction (dark / light / hybrid / toggle) with rationale. Phase 0 gate item 12 satisfied. |

### Milestone 0b: Design System Deliverables

| ID | Description | State | Depends On | Risk | Acceptance Criteria |
|----|-------------|-------|------------|------|---------------------|
| MC-004 | Color system + typography scale + panel component | done | MC-003 | medium | Color system document specifies: background tones, status palette (green/amber/red or warm equivalents), accent color (selected from teal/burgundy/amber), text colors. Typography scale specifies serif (ET Book or Source Serif), monospace (JetBrains Mono or IBM Plex Mono), sans-serif chrome. Panel component HTML/CSS exists showing borders, padding, background, card containment. |
| MC-005 | Widget vocabulary — all archetypes in HTML/CSS | done | MC-004 | medium | HTML/CSS mockups exist for every widget archetype: status indicator, sparkline, timeline, gauge/meter (≤4 custom SVG), attention card (×3 kinds), signal card, agent status card, search result card, approval card, progress bar, badge, data table. Each rendered in the chosen aesthetic direction. |
| MC-006 | Widget inventory + analog readout budget | done | MC-005 | low | Widget inventory table exists listing every widget instance across all 6 pages with columns: page, section, widget name, widget type, data source, custom/standard. Analog readout candidates categorized — ≤4 marked as custom SVG gauge, all others as standard. Total widget count documented. |
| MC-007 | Attention page mockup at real data density | done | MC-005 | medium | HTML/CSS mockup of Attention page exists showing: urgency counts strip, ≥8 sample cards (mix of system/relational/personal kinds), filters panel, quick-add input area. Uses real data patterns (not lorem ipsum). Scannable — "what needs me?" answerable in <10 seconds. |
| MC-008 | Ops page mockup at real data density | done | MC-005 | medium | HTML/CSS mockup of Ops page exists (evolved from MC-002 winning direction) showing: KPI strip (Tess status, gateway, FIF services, Healthchecks, CPU/RAM/disk), service grid with ≥6 cards, 24h timeline with ≥10 sample events, cost burn panel. "Is the house on fire?" answerable in <5 seconds. |
| MC-009 | Intelligence page mockup + nav shell | done | MC-007, MC-008 | medium | Intelligence page mockup showing both Pipeline section (KPI strip, digest panel with ≥3 signals, pipeline health) and Production section (research briefs queue, weekly intel). Nav shell mockup showing left rail with 6 page icons, badge counts, status dots, header with search bar. [AP-17 split] |
| MC-055 | Empty/error/stale state patterns + mobile viewport testing | done | MC-007, MC-008 | medium | Empty/error/stale/partial state treatments each have a distinct visual pattern in HTML/CSS. Attention + Ops mockups render readably at 375px viewport width without horizontal scroll. [AP-17 split from MC-009] |
| MC-010 | Design gate review | done | MC-009, MC-055 | high | All 12 gate checklist items (§9.1) evaluated and documented as pass/fail. Gate item 4 requires Attention, Ops, and Intelligence pages mocked at real data density (3 named pages, not generic "≥2"). [AP-11] Any failures addressed before gate passes. 6-vs-7 page decision recorded. Panel availability matrix complete for all 6 pages (§6.0 — available now / derivable / blocked on upstream). [AP-3] Operator signs off on design system for Phase 1 implementation. |

## Phase 1: Foundation + Attention-lite + Ops + Intel Pipeline

### Milestone 1: Project Scaffolding + Infrastructure

| ID | Description | State | Depends On | Risk | Acceptance Criteria |
|----|-------------|-------|------------|------|---------------------|
| MC-011 | Monorepo initialization | done | MC-010 | low | `~/openclaw/crumb-dashboard/` git repo exists. `package.json` defines workspaces for `packages/api` and `packages/web`. TypeScript configured for both packages. ESLint + Prettier configured. `npm install` succeeds. `npm run build` succeeds (empty shells). |
| MC-012 | Express API scaffold | done | MC-011 | low | `packages/api/` has: `server.ts` with Express app, route files for `/api/health`, `/api/attention`, `/api/ops`, `/api/intel`, `/api/customer`, `/api/agents`, `/api/vault`, `/api/search` (stub 501 responses). Health endpoint returns `{status: "ok", uptime, adapters: {}}`. Error logging writes to configurable log path. `npm run dev` starts API on configured port. |
| MC-013 | React + Vite scaffold with nav shell | done | MC-011 | low | `packages/web/` has: Vite config with React + TypeScript, Tailwind CSS configured, app shell with React Router, nav rail component matching Phase 0 mockup (6 page icons, badge placeholders, status dots), page route components (stub "Coming soon" for each). `npm run dev` starts dev server. Nav rail links route to correct pages. |
| MC-014 | Cloudflare Tunnel + Access setup | done | MC-012, MC-013 | medium | Cloudflare Tunnel routes to the dashboard (API + web). Cloudflare Access policy restricts to operator's email. Dashboard accessible at configured domain with authenticated session. Unauthenticated requests receive 403. |
| MC-015 | launchd service definition | done | MC-012 | low | `com.crumb.dashboard.plist` exists and is loadable via `launchctl`. Service starts the Express API (or combined dev server). Auto-restarts on crash. `launchctl list` shows the service running. |
| MC-016 | system-stats.sh + service-status.sh scripts | done | — | low | **Creation:** `_system/scripts/system-stats.sh` exists and writes CPU load, memory usage, disk utilization to `_system/logs/system-stats.json` when run manually. `_system/scripts/service-status.sh` exists and queries `launchctl list` for defined service set, writes to `_system/logs/service-status.json`. JSON output matches expected schema. JSON schemas for both files documented in CONVENTIONS.md (field names, types, example values). [AP-16] Launchd plists exist in the repo. **Deployment:** Manual step — install plists on Mac Studio, verify both run every 60s. Deployment documented in repo README or deployment notes. |
| MC-017 | Nav summary endpoint + polling infrastructure | done | MC-012, MC-013 | low | `GET /api/nav-summary` returns badge data per page (counts, status, urgency) plus per-page status computed from adapter error roll-up: `error` if any required adapter failed, `warning` if any is stale, `ok` otherwise. [AP-6] React `usePolling` hook exists — configurable interval, pauses when tab not visible, returns `{data, loading, error, lastUpdated}`. Nav rail badges update from nav-summary polling at 60s. Manual-pull pages have health strip that auto-refreshes from same endpoint. |
| MC-018 | Cross-cutting conventions: doc + shared utilities | done | MC-012, MC-013 | low | `CONVENTIONS.md` in repo root documents: UTC storage convention, local display via `Intl.DateTimeFormat`, per-page sort keys, adapter return contract `{data, error, stale}`, centralized stale detection thresholds as named constants (`STALE_SYSTEM_METRICS = 180s`, `STALE_ATTENTION_ITEM = 14d`, `STALE_DISPATCH = 1h`, etc.) [AP-5], adapter error roll-up rules (required vs optional per page) [AP-6]. **Implemented:** `writeVaultFile()` utility in API package (temp-file-then-atomic-rename, zombie `.tmp` cleanup for files >30s old, unit tested) [AP-8]. `SafeMarkdown` component in web package (DOMPurify wrapper, renders sanitized markdown, unit tested). Both are shared infrastructure used by all subsequent tasks — must be built here, not deferred. |

### Milestone 2: Ops Page

| ID | Description | State | Depends On | Risk | Acceptance Criteria |
|----|-------------|-------|------------|------|---------------------|
| MC-019 | Ops adapters: Healthchecks.io, system-stats, service-status | done | MC-012, MC-016 | low | Three adapter modules exist: `healthchecks.ts` (queries Healthchecks.io API, returns check statuses), `system-stats.ts` (reads `system-stats.json`, returns CPU/RAM/disk), `service-status.ts` (reads `service-status.json`, returns per-service status). Each returns `{data, error, stale}` triple. Each handles missing/corrupt source file gracefully. |
| MC-020 | Ops adapters: health-check log, vault-check, ops-metrics, llm-health | done | MC-012 | low | Four adapter modules exist: `health-check-log.ts` (parses `health-check.log`, returns recent events), `vault-check.ts` (runs or reads vault-check output, returns pass/fail + warnings), `ops-metrics.ts` (parses TOP-050 structured run logs, returns job stats, cost data), `llm-health.ts` (aggregates ops-metrics + dispatch telemetry by model/provider, returns per-model success rate, p95 latency, call count, degradation notes). Each returns `{data, error, stale}` triple. [AP-19] |
| MC-021 | Ops page frontend — KPI strip + service grid + LLM status | done | MC-019, MC-020, MC-013 | medium | Ops page renders: KPI strip (Tess status, gateway status, FIF services ×3, Healthchecks aggregate, CPU/RAM/disk). Service grid shows one card per launchd service with status dot, last run time, last result. Click expands card to show recent log entries. LLM Status section renders one card per provider/model showing success rate, p95 latency, call count, degradation notes. Gray/stale state for models with no recent calls. Section order per D5: Status → Gauges → Services → Timeline → LLM Status → Cost Burn. Data sourced from ops adapters. [AP-19] |
| MC-022 | Ops page frontend — 24h timeline + cost burn + auto-refresh | done | MC-021 | medium | Ops page additionally renders: 24h timeline with color-coded event dots (heartbeats, alerts, mode transitions), cost burn panel (daily/weekly spend vs ceiling, per-job breakdown). Page auto-refreshes every 30s via polling hook. Stale data indicators show when adapter data is older than expected. Nav-summary controller updated to source Ops adapter data for page badges and status. [DR-A5] |
| MC-023 | Ops adapter unit tests | done | MC-019, MC-020 | low | Unit tests exist for all 7 Ops adapters (including llm-health). Tests use fixture data (sample JSON files, sample log content). Each test verifies: normal output, missing file handling, corrupt data handling, stale detection. All tests pass. [AP-19] |

### Milestone 3: Intelligence Pipeline Section

| ID | Description | State | Depends On | Risk | Acceptance Criteria |
|----|-------------|-------|------------|------|---------------------|
| MC-024 | FIF SQLite adapter | done | MC-012 | medium | `fif-sqlite.ts` adapter reads FIF state DB. Returns: signals (with tier, source, timestamp, content), triage results, source metadata, cost telemetry. Handles DB-locked and DB-missing errors gracefully. Returns `{data, error, stale}`. |
| MC-025 | Pipeline health adapter | done | MC-012 | low | `pipeline-health.ts` adapter reads FIF state DB and/or log files. Returns: circuit breaker status per service, last capture/attention/feedback run times, error rates, stale backlog count. Returns `{data, error, stale}`. |
| MC-026 | Intelligence Pipeline section frontend | done | MC-024, MC-025, MC-013 | medium | Intelligence page Pipeline section renders: KPI strip (signals today/week with sparkline, per-source breakdown, triage distribution, cost today vs ceiling). Digest panel shows signal cards grouped by source/tier/topic. Pipeline health panel shows circuit breaker status and run times. Manual pull refresh. Health strip auto-refreshes via nav-summary polling. Nav-summary controller updated to source Intel adapter data for page badges. [DR-A5, DR-A6] |
| MC-027 | Signal detail panel + pipeline health polish | done | MC-026 | low | Signal detail panel opens on card click — shows full content, enriched context for researched items. Pipeline health panel shows error rates and stale backlog. Page layout matches Phase 0 Intelligence mockup. |
| MC-028 | M-Web parity gate + Intelligence adapter tests | done | MC-064 | high | M-Web parity gate evaluated: Pipeline section delivers digest display, signal rendering, pipeline health, source/tier/topic filtering, and triage actions (skip/delete immediate, promote queued) matching M-Web spec core features. **Pass criteria:** operator uses Pipeline section for 3 consecutive days for signal triage instead of Telegram + Crumb session; page load + manual refresh under 400ms. [AP-2] Gate result documented. If gate fails, M-Web revert plan activated. Adapter unit tests exist for FIF SQLite and pipeline health adapters. All tests pass. |

### Milestone 3b: Intel Pipeline Triage Actions

| ID | Description | State | Depends On | Risk | Acceptance Criteria |
|----|-------------|-------|------------|------|---------------------|
| MC-061 | `dashboard_actions` table + FIF adapter update | done | MC-024 | low | `dashboard_actions` table created in FIF SQLite DB (schema per §8.6). FIF SQLite adapter updated to LEFT JOIN `dashboard_actions` on `canonical_id`, exposing `dashboard_action` and `action_created_at` fields on each signal. Signals with `skip` or `delete` actions are excluded from default signal list (available via separate query for audit). Table creation runs as an idempotent migration on API startup. |
| MC-062 | Skip + delete endpoints | done | MC-061 | low | `POST /api/intel/:canonical_id/skip` writes `{action: 'skip'}` row to `dashboard_actions`. Returns updated item state. `DELETE /api/intel/:canonical_id` writes `{action: 'delete'}` row to `dashboard_actions` and removes matching `_openclaw/inbox/feed-intel-*.md` file if present (match by canonical_id in frontmatter). Returns confirmation. Both endpoints validate that `canonical_id` exists in `posts` table (404 if not). Both are idempotent (UNIQUE constraint, INSERT OR IGNORE). Unit tests cover: valid skip, valid delete, delete with inbox file, delete without inbox file, unknown canonical_id, duplicate action. |
| MC-063 | Promote-queue endpoint | done | MC-061 | low | `POST /api/intel/:canonical_id/promote` accepts optional `{kb_tag?}`. Validates `kb_tag` against canonical kb/ tag list if provided. Writes `{action: 'promote'}` row to `dashboard_actions` with optional `metadata` JSON column for kb_tag override. Returns queued confirmation with promote queue count. Rejects if item already has a `promote` action (409 Conflict). Unit tests cover: valid promote, promote with kb_tag, promote with invalid kb_tag, duplicate promote, unknown canonical_id. |
| MC-064 | Intel page triage UI — action buttons + promote queue | done | MC-062, MC-063, MC-026 | medium | Signal cards on Intelligence Pipeline section show triage action buttons: skip (dimmed icon), delete (trash icon), promote (up-arrow icon). Promote button optionally shows kb/ tag selector (pre-populated from FIF triage tags mapped through canonical lookup). Actions hit respective endpoints; card updates in-place on success (skip/delete: card fades out; promote: card shows "queued" badge). Promote queue indicator visible in Pipeline section header showing count of pending promotions. Consumed promotions show "promoted" status with wikilink-style reference to resulting signal-note path (populated after feed-pipeline skill processes). Undo available for 5s after skip/delete (re-enables card, deletes `dashboard_actions` row). |
| MC-065 | Feed-pipeline skill — dashboard-flagged entry path | done | MC-061 | medium | Feed-pipeline SKILL.md updated with new Step 0: "Check for dashboard-flagged promotions." Skill queries `dashboard_actions WHERE action='promote' AND consumed_at IS NULL` joined with `posts` for full metadata. Flagged items skip permanence evaluation (Step 4 Q1-Q3 — human already decided) but run full promotion workflow: source_id generation, frontmatter construction with kb_tag from metadata override (or auto-mapped from triage tags), signal-note write, MOC registration, knowledge retrieval, project cross-posting (Step 5 Q4), calibration logging. On success, sets `consumed_at` to current timestamp. Skill reports dashboard-flagged promotions separately in the Step 1 count summary (e.g., "Dashboard-queued: 3 promotions"). Existing inbox-based workflow unchanged — both entry paths coexist. |
| MC-066 | Feed-pipeline skill → dashboard sync-back | done | MC-065 | low | When feed-pipeline processes an inbox item (auto-promote, Tier 2 action extract, or review-queue routing), it writes a corresponding row to `dashboard_actions` with `consumed_at` already set (action = 'promote' for promoted items, 'skip' for Tier 3/review-queue skips). Dashboard's existing LEFT JOIN filters these items from "Recent Signals" immediately. Covers: auto-promote (Step 5), Tier 2 extraction (Step 3), and TTL-expired items (if feed-inbox-ttl.sh is extended). No new tables — reuses `dashboard_actions` with the existing schema. Feed-pipeline skill SKILL.md updated with sync-back step after each processing path. |
| MC-068 | Research triage action — endpoint + UI button | done | MC-062 | low | `POST /api/intel/:canonical_id/research` writes `{action: 'research'}` row to `dashboard_actions` with optional `metadata` JSON (operator notes, priority). Returns queued confirmation. Rejects if item already has a `research` action (409 Conflict). Signal cards on Intelligence Pipeline section show research button (magnifying glass icon). Action hits endpoint; card shows "queued for research" badge. Tess overnight research cron (TOP-046) queries `dashboard_actions WHERE action='research' AND consumed_at IS NULL` to find items. On processing, Tess sets `consumed_at`. Unit tests: valid research action, duplicate action, unknown canonical_id. Mirrors promote pattern (MC-063). |

### Milestone 4: Attention-lite Page

| ID | Description | State | Depends On | Risk | Acceptance Criteria |
|----|-------------|-------|------------|------|---------------------|
| MC-029 | Attention aggregator — single source (dispatch pending) | done | MC-012, MC-023 | high | `attention.ts` aggregator reads dispatch state files from `_openclaw/state/dispatch/`. Identifies dispatches in `blocked` state or with review-needed flag. Normalizes to attention-item schema. Returns sorted list (urgency → age). Handles empty directory and malformed files. Returns `{data, error, stale}`. **Integration test passes for single-source aggregation before MC-030 expands to multi-source.** [DR-A3, DR-A4] |
| MC-030 | Attention aggregator — add remaining Phase 1 sources | done | MC-029 | high | Aggregator additionally reads: FIF health flags (from pipeline-health adapter), vault-check warnings, Healthchecks.io stale pings, **and `_inbox/attention/` for manually created items** (quick-add visibility — items written by MC-031 must appear immediately). [AP-4] Each source produces attention items normalized to common schema. Aggregator merges, deduplicates by `attention_id` and `source_ref`, sorts by urgency → age. Integration test verifies cross-source merge correctness. Test verifies aggregator ignores items in `Archived/attention/`. [AP-7] |
| MC-031 | Quick-add endpoint (POST /attention) | done | MC-012 | medium | `POST /api/attention` accepts `{title, domain, urgency, kind, due?}`. Generates `attention_id` as UUID v4 (`crypto.randomUUID()`). [AP-1] Writes attention-item markdown note to `_inbox/attention/` using `writeVaultFile()` (temp + atomic rename). Returns created item. Validates required fields. Rejects invalid urgency/kind/domain values. |
| MC-032 | Attention page frontend — cards + urgency strip | done | MC-030, MC-013 | medium | Attention page renders: urgency counts strip (Now/Soon/Ongoing/Awareness with counts), card list sorted by urgency then age. Each card shows: title, source domain tag, age, urgency badge, kind indicator. Quick-add form at top creates items via POST endpoint. Quick-add pre-fills domain with last-used value (localStorage), urgency defaults to 'soon', kind defaults to 'personal' for manual items. Only title is required to submit. [AP-12] |
| MC-033 | Attention page frontend — filters + views + completed feed | done | MC-032 | medium | Attention page additionally has: filter panel (domain, urgency, action type, source, kind). Switchable views: triage (default), domain-grouped, source-grouped. Collapsible completed/dismissed feed at bottom. Page auto-refreshes at 60s via polling hook. Nav-summary controller updated to source Attention aggregator data for page badges. [DR-A5] |
| MC-034 | Attention adapter + aggregator tests | done | MC-030, MC-031 | low | Unit tests for aggregator (single-source and multi-source). Integration test verifying dedup across sources, sort order correctness, and source_ref collision handling. Unit test for quick-add endpoint (valid input, invalid input, file write verification). All tests pass. |

### Cross-Project + Vault Registration (M1/M4 scope)

| ID | Description | State | Depends On | Risk | Acceptance Criteria |
|----|-------------|-------|------------|------|---------------------|
| MC-053 | Cross-project amendments for M-Web / A2A absorption | done | MC-011 | low | FIF action plan updated to cross-reference mission-control for M-Web tasks (FIF-W01–W12). A2A task list updated to mark A2A-015.1/015.2/015.3 as superseded by mission-control. Both project run-logs have entries noting the absorption. No orphaned tasks referencing standalone M-Web or A2A-015.x remain without cross-reference. |
| MC-054 | Register attention-item type in vault conventions + vault-check | done | MC-031 | low | `type: attention-item` registered in `_system/docs/file-conventions.md` with required fields documented. `vault-check.sh` updated with validation rule for attention-item frontmatter (required fields: attention_id, kind, domain, status, urgency, schema_version; valid enum values). `vault-check.sh` passes with sample attention-item notes in `_inbox/attention/`. |
| MC-056 | Tess mechanic health monitoring integration | done | MC-012 | low | Tess mechanic's hourly pipeline monitoring check (TOP-011 extension) includes `GET /api/health` for the dashboard. If health endpoint is unreachable for 2 consecutive mechanic checks, Tess alerts via Telegram. SC-7 verified: dashboard is a monitored service. [AP-3] |
| MC-057 | Playwright smoke test | done | MC-012, MC-013 | low | Single Playwright E2E test exists: navigates to dashboard, verifies nav rail renders with 6 page icons, routes to each page without error, health endpoint returns 200. Test runs in CI or locally via `npm run test:e2e`. [AP-3, AP-18] |
| MC-058 | Cloudflare Access verification middleware | done | MC-014 | medium | `verifyCfAccessHeaders()` Express middleware exists. Validates CF Access JWT headers on protected routes. Applied to `/api/customer` routes. Nav-summary excludes customer data when headers absent. Unit tested with valid/invalid/missing header fixtures. Reusable — any route can opt in. [DR-A1] |
| MC-059 | Production build + Express static serve | done | MC-013, MC-018 | low | `npm run build` in web package produces Vite production output in `dist/`. Express API serves `dist/` via `express.static()` in production mode. launchd plist runs production build + start (not dev server). Dashboard renders pages correctly through Cloudflare Tunnel in prod mode. [DR-A2] |
| MC-060 | Investigation: structured per-call LLM telemetry | done | — | medium | **Investigation, not implementation.** Determine where structured per-call telemetry (model, tokens, cost, latency, success/failure) lives for non-FIF API usage: Tess voice (Haiku via OpenClaw), mechanic (local qwen3-coder), Sonnet dispatches, Opus code reviews. If it doesn't exist, propose the lightest way to add it. Output: investigation note in `Projects/tess-operations/` or `Projects/mission-control/design/` with findings and recommendation. Blocks: Ops page full Cost Burn (currently FIF-only) and LLM Status panel (currently in error state). Related: TOP-050 (ops metrics harness), spec F13. |

### Phase 1 Retrospective

| ID | Description | State | Depends On | Risk | Acceptance Criteria |
|----|-------------|-------|------------|------|---------------------|
| MC-035 | Phase 1 retrospective | done | MC-023, MC-028, MC-034 | low | **Prerequisite:** At least 7 calendar days have elapsed since the last Phase 1 milestone was completed (usage period, not just task completion). Retrospective document exists at `Projects/mission-control/progress/phase-1-retro.md`. Contains: daily usage assessment, per-page attention analysis, Attention-lite value evaluation, SC-1/SC-3/SC-5 status, React component testing decision (PC-7), SSE upgrade decision (PC-9), Phase 2 scope confirmation or adjustment. **Retro explicitly considers "pause after Phase 1 and deepen existing pages" as an acceptable outcome; decision recorded.** [AP-14] Operator has reviewed and approved Phase 2 scope. |

## Phase 2: Full Attention + Knowledge

### Milestone 5: Full Attention Schema + Multi-Source Aggregator

| ID | Description | State | Depends On | Risk | Acceptance Criteria |
|----|-------------|-------|------------|------|---------------------|
| MC-036 | Full attention-item schema validation | done | MC-035 | medium | Schema validation module exists. Validates attention-item frontmatter against §7.1 schema (all fields, valid enums, schema_version). Handles backward compatibility for missing fields in older versions (R2-10). Reports validation errors without rejecting items (log + flag as degraded). **Recurring/practice items:** Schema supports an `item_mode` field distinguishing one-shot items (checked = goal complete) from recurring items (checked = "did it today," regenerates tomorrow). This enables long-running goals (e.g., daily reading toward finishing a book) where daily completion ≠ goal completion. The upstream attention-manager skill owns generation/carry-forward logic; the schema encodes the distinction so the dashboard can render and toggle appropriately. |
| MC-037 | Expanded vault scanner (all item kinds) | done | MC-036 | medium | Vault scanner reads `_inbox/attention/` for all item kinds (system, relational, personal). Correctly parses Tess-generated items, manual items, and overlay-generated items. Validates each against schema. Returns typed attention items with source metadata. Handles `deferred_until` — deferred items hidden until date arrives. |
| MC-038 | Aggregator multi-source expansion | done | MC-037, MC-030 | high | Aggregator merges vault-scanned items with existing Phase 1 sources (dispatch, FIF health, vault-check, Healthchecks). Full dedup by `attention_id` (unique) and `source_ref` (24h window). Staleness indicator on items untouched >14 days. Total item count, per-kind count, per-urgency count available for nav-summary. |
| MC-039 | Attention page enhancements for full schema | done | MC-038 | medium | Attention page displays all three item kinds with distinct card styling. Dedup display — if same source_ref appears, show merged view. Staleness indicator visible on stale items. Kind-specific action buttons on cards. Deferred items appear with "deferred until [date]" badge when deferred_until has passed. **Recurring items:** Cards with `item_mode: recurring` show a repeat indicator and "done for today" checkbox semantics (vs. "completed" for one-shot items). Checked recurring items render as completed-today rather than fully resolved. **Promote from Deferred:** Deferred items have a "promote to Focus" action button. Clicking promotes the item: API moves it from Deferred section to Focus section in the daily markdown (as unchecked `- [ ]` item with available metadata), returns updated daily plan. UI moves the card from the collapsible Deferred section into the active Focus list without a full page reload. |

### Milestone 6: Knowledge / Vault Page

| ID | Description | State | Depends On | Risk | Acceptance Criteria |
|----|-------------|-------|------------|------|---------------------|
| MC-040 | Knowledge adapters — QMD, AKM feedback, vault-check, project health, vault gardening | done | MC-012 | medium | Five adapter groups: `qmd.ts` (QMD collection stats — doc count, chunk count, last index update, growth rates, chunk density, parsing anomalies), `akm-feedback.ts` (parses akm-feedback.jsonl — hit rate, most/never surfaced sources, surfacing count, dead knowledge candidates), `vault-check-summary.ts` (pass/fail, warning count, last run, tag hygiene — tags on 1-2 notes, tags with no MOC parent, distribution skew), `project-health.ts` (scans project-state.yaml files — velocity indicators, stall detection), `vault-gardening.ts` (orphan detection — notes with no inbound wikilinks/MOC/tags; stale source candidates — time-sensitive content not referenced in 6+ months). Each returns `{data, error, stale}`. [AP-20] |
| MC-041 | Search endpoint (shared) | done | MC-040 | medium | `GET /api/search?q=...` queries QMD across all collections. Returns results with collection badge, relevance score, snippet. Same endpoint used by nav search bar and Knowledge page. Results render in unified card format. |
| MC-042 | Knowledge page frontend | done | MC-040, MC-041, MC-013 | medium | Knowledge page renders: vault health KPIs (total notes, MOC coverage, vault-check status), AKM panel (hit rate over rolling 10 sessions, most-surfaced sources, never-surfaced sources, "review stale sources" action link), project health grid (active projects with velocity, stall flags), tag distribution visualization, Vault Gardening section (dead knowledge list with "archive" action links, orphan detection panel, stale source candidates, tag hygiene panel, QMD collection health). Each gardening finding produces an attention item or direct action link. Manual pull refresh + health strip auto-refresh. Nav-summary controller updated to source Knowledge adapter data for page badges. [DR-A5, AP-20] **"Review stale sources" click calls `POST /attention` with pre-filled fields: `kind=personal`, `domain=learning`, `source_system=akm`, `urgency=ongoing`, title including the stale source names. Success confirmation shown. Test verifies created file.** [AP-13] |
| MC-043 | Knowledge adapter unit tests | done | MC-040, MC-041 | low | Unit tests for all Knowledge adapters with fixture data. Search endpoint test with mock QMD responses. All tests pass. |
| MC-072 | AKM consumption tracking — populate paths_hit/paths_miss | done | MC-040 | medium | AKM feedback JSONL has `session-end` entries with `paths_hit` and `paths_miss` fields, but they are always empty arrays. `session_read` echoes `session_surfaced` (all hit rates = 1.0) — no real consumption differentiation. **Instrumentation:** At session end, `knowledge-retrieve.sh` (or session-end hook) must compare surfaced paths against actual Read tool calls during the session to populate `paths_hit` (surfaced AND read) and `paths_miss` (surfaced but never read). **Dashboard:** AKM adapter + panel updated to show consumption metrics: per-source utilization rate (times consumed / times surfaced), overall consumption rate across sessions, never-consumed sources (surfaced but never read — dead weight in retrieval). **Depends on:** AKM project's `knowledge-retrieve.sh` and session-end feedback loop (cross-project — changes to `_system/scripts/`). |

### Milestone 7: Attention Status Updates

| ID | Description | State | Depends On | Risk | Acceptance Criteria |
|----|-------------|-------|------------|------|---------------------|
| MC-044 | PATCH /attention/:id endpoint | todo | MC-038 | medium | `PATCH /api/attention/:id` accepts `{status?, deferred_until?}`. Reads current attention-item file **and captures mtime**. Validates transition (open→in-progress→done/deferred/dismissed). Updates frontmatter `status` and `updated` fields. **If mtime has changed between read and write (another actor edited the file), returns 409 Conflict with current file state.** [AP-8] Writes via `writeVaultFile()`. Returns updated item. Rejects invalid transitions. Returns 404 for unknown IDs. |
| MC-045 | Attention page inline status update UI | todo | MC-044, MC-039 | low | Each attention card has inline action buttons: mark done, defer (with date picker), dismiss. Status changes hit PATCH endpoint. Card updates in-place on success. Undo available for 30s after transition (re-PATCHes to previous status). Completed/dismissed items move to bottom feed. |
| MC-071 | Nav-summary attention status — rewire to daily plan + goals | done | MC-069 | low | Nav-summary `/api/nav-summary` attention page status currently derives from the old `getAttention()` vault-item aggregator (urgency counts from `_inbox/attention/`). Rewire to reflect the new data sources: daily plan completion progress (items checked / total), overdue goals, carry-forward escalation count. Badge count should reflect unchecked focus items. Status logic: `error` if escalated items exist, `warn` if <50% focus items checked after 3pm, `ok` otherwise. Remove HealthStrip "stale data detected" false positive caused by legacy aggregator. |
| MC-069 | Quick Add repurpose — daily focus items only | done | MC-067 | medium | **Repurpose Quick Add from vault-attention-item creator to daily plan focus item input.** Title + domain fields. `POST` appends a `- [ ]` item to today's Focus section in `_system/daily/YYYY-MM-DD.md` with domain tag. Item appears in Daily Plan panel immediately (triggers refresh). Handles missing daily file gracefully (error: "No plan for today"). Mtime conflict detection on daily file write. **Remove:** Old `POST /api/attention` endpoint and `_inbox/attention/` vault-item creation. `writeVaultFile` + `attentionInboxDir` unused after removal. **Tests:** Focus append (new item appears in daily file), missing daily file error, mtime conflict detection. |
| MC-070 | Goals panel — list, add, complete | done | MC-067 | medium | **Dedicated Goals section on Attention page.** **Adapter:** `goals.ts` reads `_system/docs/goal-tracker.yaml`, parses all goals (id, domain, description, horizon, target_date, status, progress). Returns `{data, error, stale}`. **API:** `GET /api/attention/goals` returns all goals. `POST /api/attention/goals` creates a new goal — accepts description, domain, horizon, target_date; assigns next sequential ID (G4, G5...), sets status `active`, empty progress; writes back to goal-tracker.yaml with mtime conflict detection. `PATCH /api/attention/goals/:id` accepts `{status?: 'completed'|'active', progress?: string}` — updates goal in YAML, mtime conflict detection. **Cascade:** When a goal is marked completed, also check off (`[x]`) any Focus items in today's daily plan that reference that goal ID (e.g., `Goal: G2`). Uses daily-attention adapter's toggle logic. Returns list of cascaded item titles in response. **UI:** Goals panel on Attention page below Daily Plan. Lists all active goals as cards showing ID, description, domain badge, horizon, target date, progress. Each card has a "Complete" action. Completed goals move to a collapsed "Completed" sub-section. Inline add form (description + domain + horizon + target date) at bottom of panel. **Tests:** adapter parse, goal creation with ID sequencing, goal completion status update, mtime conflict. |
| MC-067 | Daily Attention panel — adapter + UI + write-back | done | MC-032, XD-014 | medium | **Adapter:** `daily-attention.ts` reads `_system/daily/YYYY-MM-DD.md` for today's date. Parses Focus items (checkbox state, description, domain, goal ref, carry-forward count), Domain Balance, Carry-Forward, and Goal Alignment sections. Returns `{data, error, stale}`. Handles missing artifact gracefully (returns empty with "no plan today" indicator). **API:** `PATCH /api/attention/daily/:date/items/:index` accepts `{checked?: boolean, deferred?: boolean, note?: string}`. Reads the daily artifact, toggles checkbox state (`- [ ]` ↔ `- [x]`), writes back via `writeVaultFile()` with mtime conflict detection. **UI:** "Daily Plan" panel on Attention page renders today's Focus items as interactive cards. Each card shows: description, domain tag, goal badge, carry-forward indicator, "Why now" expandable. Actions: checkbox toggle (mark complete), defer button (removes from today, adds to Deferred section with reason prompt), reorder drag (optional). Domain Balance summary visible below cards. Panel header shows artifact date + soak/freshness indicator. If no artifact exists, panel shows prompt: "No plan for today — run 'plan my day' in Crumb." Carry-forward mechanics remain Crumb's responsibility — dashboard only reads/writes checkbox state, never generates or re-curates. **Tests:** adapter unit test (valid artifact, missing artifact, malformed markdown), PATCH endpoint test (toggle checkbox, conflict detection, invalid index). |

## Phase 3: Agent Activity + Customer/Career + Intel Production

### Milestone 8: Intelligence Production Section

| ID | Description | State | Depends On | Risk | Acceptance Criteria |
|----|-------------|-------|------------|------|---------------------|
| MC-046 | Intelligence Production adapters | todo | MC-012 | low | `inbox-scanner.ts` adapter reads `_openclaw/inbox/` for research briefs, intelligence briefs, brainstorm files, tuning recommendations. Categorizes by type. Returns list with title, type, date, status (pending/reviewed). Returns `{data, error, stale}`. |
| MC-047 | Intelligence Production section frontend | todo | MC-046, MC-026 | medium | Intelligence page Production section renders: overnight research briefs queue (pending/completed), weekly intelligence brief (latest + historical), connections brainstorm (featured card when new), builder ecosystem radar. Manual pull refresh. Both Pipeline and Production sections render correctly on merged Intelligence page. |

### Milestone 9: Agent Activity Page

| ID | Description | State | Depends On | Risk | Acceptance Criteria |
|----|-------------|-------|------------|------|---------------------|
| MC-048 | Agent Activity adapters | todo | MC-012 | medium | Adapters: `dispatch-state.ts` (reads `_openclaw/state/dispatch/` — lifecycle status, correlation ID, duration, tokens, cost), `tess-context.ts` (reads `_openclaw/state/tess-context.md` — current priorities, staleness), `cost-aggregation.ts` (aggregates ops-metrics + dispatch costs by model, day/week/month). Each returns `{data, error, stale}`. |
| MC-049 | Agent Activity page frontend | todo | MC-048, MC-013 | medium | Agent Activity page renders: agent status cards (Tess Voice, Mechanic, Crumb with model/status/last interaction), dispatch log (filterable by agent/status/project, shows lifecycle status, duration, cost), cost dashboard (token usage over time by model, budget vs actual), context model panel (tess-context.md rendered with staleness indicator). Auto-refresh at 60s. |

### Milestone 10: Customer / Career Page

| ID | Description | State | Depends On | Risk | Acceptance Criteria |
|----|-------------|-------|------------|------|---------------------|
| MC-050 | Customer/Career adapters | todo | MC-012 | medium | `dossier-scanner.ts` adapter reads customer-intelligence dossiers. Returns: account name, engagement status, last touch date, dossier completeness score, comms cadence — but NOT raw dossier content or PII. Route protected by `verifyCfAccessHeaders()` middleware (MC-058, C7) — adapter does not handle auth. Returns `{data, error, stale}`. [DR-A1] |
| MC-051 | Customer/Career page frontend | todo | MC-050, MC-013 | medium | Customer page renders: account dashboard with health indicators, dossier completeness bars. Blocked panels (relationship heat map, pre-brief, career positioning) show placeholder treatment per §6.0 with dependency labels. Privacy notice visible. Manual pull refresh + health strip auto-refresh. Nav-summary controller updated to source Customer adapter data for page badges. [DR-A5] |
| MC-052 | Phase 3 adapter tests | todo | MC-046, MC-048, MC-050 | low | Unit tests for all Phase 3 adapters (inbox-scanner, dispatch-state, tess-context, cost-aggregation, dossier-scanner). Privacy constraint test: verify customer endpoints reject requests without Cloudflare Access headers. All tests pass. |
