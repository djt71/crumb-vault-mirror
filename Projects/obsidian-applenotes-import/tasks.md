---
project: obsidian-applenotes-import
domain: software
type: task-list
skill_origin: action-architect
created: 2026-04-27
updated: 2026-04-27
tags:
  - tasks
---

# Apple Notes Import — Tasks

29 atomic tasks across 9 phases (M0 spikes + M1–M8 implementation). Each task ≤5 file changes. IDs preserved from spec rev 4 (OAI-001..023, 016a..e); spike IDs OAI-024..027 added during PLAN.

## M0: PLAN Spikes

| ID | Description | State | Depends On | Risk | AC |
|----|-------------|-------|------------|------|----|
| OAI-024 | A1 perf spike — measure `every note` listing latency. Stage 0: `tell application "Notes" to count notes` to size the user's library; if ≥1k, time the full `every note` listing. If <1k, document the user's library size and treat A1 as "deferred to first-run measurement with paginated/streamed fallback path." Bail rule: if listing >10s, return to PLAN risk review. Output: `design/probes/a1-list-perf.applescript` + `design/probes/a1-perf-result.md`. If perf triggers contingency, also write `design/decisions/001-modal-loading-strategy.md`. | not started | — | medium | Probe artifact + result captured. Bucketed outcome: ≤5s (simple modal), 5–10s (contingency: pagination/streaming required in OAI-013), >10s (return to PLAN). Decision recorded if non-simple outcome. |
| OAI-025 | A3 attachment-HTML spike — fetch `body of note` for one attachment-bearing note, capture the raw HTML, confirm parser tolerates it. Stage 0: identify or create one attachment-bearing test note (image is the easiest); if user library has zero, create one via UI then run probe. Bail rule: if `body of note` raises an error or returns binary content (not HTML string), document and revisit conversion strategy. Output: `design/probes/a3-attachment-html.applescript` + `design/probes/a3-result.md` (with raw HTML excerpt, redacted if needed). | not started | — | low | One real attachment-bearing note's HTML captured. Confirmed that turndown + custom node-filter can drop the attachment object cleanly without crashing. Or: bail with documented alternative (e.g., regex pre-strip in OAI-009). |
| OAI-026 | A5 TCC denial detection spike — `tccutil reset AppleEvents`, then run `osascript -e 'tell application "Notes" to get name of every note'` and capture exact stderr + exit code. Then re-grant via UI. Stage 0: capture the denial-mode output. Output: `design/probes/a5-tcc-denial.sh` + `design/probes/a5-result.md` with exact stderr text and error code (-1743 / -10000 / other) for OAI-019's denial-detection regex. | not started | — | medium | Exact stderr + error code captured under denial. Detection regex for OAI-019 keyed to real evidence, not guessed. Re-grant flow tested at end of probe (leaves user's TCC state functional). |
| OAI-027 | A6 locked-note error spike — create a locked test note in Notes app (set password if not already set), run `tell application "Notes" to get body of note id X` against it, capture the AppleScript error shape. Stage 0: capture error text + code. Output: `design/probes/a6-locked-note.applescript` + `design/probes/a6-result.md` for OAI-006's skip-and-log path. | not started | — | low | Error text + code captured. OAI-006's `{skipped: 'locked'}` detection keyed to real evidence. |

**M0 exit gate:** All 4 spike artifacts present. Decisions recorded in `design/decisions/` if any spike changed a downstream task's approach. M1 may not start until M0 closed.

## M1: Foundation

| ID | Description | State | Depends On | Risk | AC |
|----|-------------|-------|------------|------|----|
| OAI-001 | Repo build scaffold — `package.json`, `tsconfig.json`, `esbuild.config.mjs`, `eslint.config.mjs`. Pin `eslint-plugin-obsidianmd` (PLAN decision: `0.1.9` template default vs `0.2.4` latest — record in `design/decisions/002-eslint-plugin-version.md`). Files: 4. | not started | — | low | `npm install && npm run build` produces `main.js`. `npm run lint` exits 0 with `eslint-plugin-obsidianmd` enabled. Plugin version pinned in `package.json`. Decision recorded. |
| OAI-002 | Plugin skeleton + minAppVersion research + platform-gate early-return. Files: `manifest.json`, `versions.json`, `src/main.ts` (3). Research minAppVersion against API surface used (`Platform`, `vault.create`, `normalizePath`, `FileSystemAdapter`, `containerEl.createEl`, `vault.on('rename')`, `vault.on('delete')`); set to concrete `X.Y.Z` before TASK lock. `onload()` early-returns on `Platform.isMobile === true` OR `!Platform.isMacOS` BEFORE any side effects. | not started | OAI-001 | low | Plugin loads in test vault on macOS. `onunload` clean (no leaked listeners). minAppVersion is a concrete `X.Y.Z`. Off-platform smoke test: mobile-OS reports a single one-shot Notice and registers no commands/ribbon/settings-tab. |
| OAI-003 | Settings tab + path validation + Re-check button. Files: `src/settings.ts`, `src/main.ts` wiring (2). Settings: target folder, receipts folder, show-already-imported default, dry-run default, debug-mode toggle. Paths normalized + validated on save. "Re-check permission" button wired as user-initiated TCC probe. | not started | OAI-002 | low | All v1 settings persist across reload. Paths normalized via `normalizePath`; invalid (empty / illegal characters) rejected with inline error. "Re-check permission" button visible and wired (handler stub OK at this stage; OAI-019 fills the body). |

**M1 exit gate:** Plugin loads cleanly in test vault. Off-platform smoke test passes. Lint clean.

## M2: AppleScript Bridge

| ID | Description | State | Depends On | Risk | AC |
|----|-------------|-------|------------|------|----|
| OAI-004 | osascript runner — `src/applescript/runner.ts` + unit test (2). Wraps `child_process.execFile` with stderr / timeout / exit-code surfacing. Returns `{stdout, stderr, code}` typed. | not started | OAI-001 | medium | Wrapper invokes a fixture `.applescript`; surfaces stderr; respects timeout (test with sleep 60); surfaces non-zero exit. Adversarial test (script returns malformed JSON) does not crash the runner. |
| OAI-005 | List notes script + parser — `src/applescript/list-notes.applescript` + `src/applescript/list-notes.ts` (2). Returns `[{id, name, account, folder, created, modified, attachmentCount, isLocked}]`. Note handles addressed by id (per probe-derived note 1). `attachmentCount` informational; `isLocked` flagged for OAI-006 to skip. | not started | OAI-004 | medium | Returns a typed array against a real test vault Apple Notes library (size-flexible — works on 1-note or 100-note libraries). Locked notes marked `isLocked: true`. Folder iteration used for membership (per probe-derived note 2). No by-name selectors. |
| OAI-006 | Fetch note body script + parser — `src/applescript/fetch-body.applescript` + `src/applescript/fetch-body.ts` (2). Returns `{id, name, account, folder, created, modified, bodyHtml, attachmentCount}`. Locked notes return `{skipped: 'locked'}` keyed to OAI-027 evidence. Parser tolerates fragment HTML and malformed input. | not started | OAI-004, OAI-027 | medium | Returns typed result for a real note. Locked-note path returns the documented skip shape. Malformed-input adversarial test does not crash parser. Note handles by id. |
| OAI-007 | **Soft-delete script + wrapper** — `src/applescript/soft-delete.applescript` + `src/applescript/soft-delete.ts` (2). Production-quality wrapper of the G1-validated probe. Note handles by id. | not started | OAI-004 | high | Soft-delete a probe note: deleted note appears in Recently Deleted within 2s; `id` remains queryable from Recently Deleted folder; integration test re-runs the G1 probe pattern via the production wrapper and passes. |

**M2 exit gate:** G1 re-test passes via production wrapper. All 4 scripts return typed parsed results.

## M3: Conversion & Vault Writes

| ID | Description | State | Depends On | Risk | AC |
|----|-------------|-------|------------|------|----|
| OAI-009 | HTML→Markdown converter (Apple-Notes-aware, attachment-dropping) — `src/convert/preprocess.ts`, `src/convert/html-to-md.ts`, golden-output tests (3). Pre-processor normalizes Apple-Notes-specific HTML (checklist objects, Apple-specific spans/divs) BEFORE turndown. Turndown configured with custom node-filters that **catch** unknown tags (turndown's default is to silently drop — explicit filter required). Attachment objects (image, file ref, drawing canvas, audio, video, web bookmark) dropped with `<!-- [v1: attachment dropped: <type-or-name>] -->` placeholder; counted into `source_had_attachments` and `import_warnings.attachments_dropped`. Severe-tier escalation: substantive body content cannot be represented (e.g., table collapses to empty). Decide turndown vs turndown+sanitize-html (record in `design/decisions/003-conversion-stack.md`). | not started | OAI-006, OAI-025 | medium | Headings, lists, links, bold/italic, GFM tables, code blocks, GFM checklists round-trip. ≥10 golden tests pass (synthesized + any captured Apple Notes HTML from OAI-025). Attachment-drop placeholder appears for each attachment kind. Severe-tier triggers on the empty-table case. Decision doc written. |
| OAI-010 | Filename sanitizer + collision policy — `src/vault/filename.ts` + tests (1+tests). Uses `normalizePath`. Strips macOS+Obsidian-illegal characters. Collision policy: title→sanitized name; suffix `-2`, `-3`, … on collision; canonical chosen path stored in index. Re-import override creates new unique file (never overwrite silently). | not started | — | low | Sanitizer round-trips a curated set of edge-case titles (slashes, colons, leading dots, leading/trailing spaces, ≥255 chars, non-ASCII). Collision suffix sequence is deterministic. Re-import path produces a fresh unique filename. |
| OAI-011 | Vault writer (markdown only) — `src/vault/writer.ts` + frontmatter helper (2). Creates target folder idempotently via `app.vault.createFolder` (catches "already exists"). Writes markdown via `app.vault.create`. Frontmatter matches LD-09 exactly (incl. `source_had_attachments`). Captures **expected pre-write content hash** for OAI-016b verify gate. **No binary writes in v1.** | not started | OAI-009, OAI-010 | medium | Vault file created with exact LD-09 frontmatter. `source_had_attachments` count matches input. Pre-write hash captured (test reads written file, recomputes hash, asserts equal). Folder creation idempotent. |

**M3 exit gate:** Golden tests pass. Vault write produces correct frontmatter + verifiable pre-write hash.

## M4: Import Index (parallel to M2/M3)

| ID | Description | State | Depends On | Risk | AC |
|----|-------------|-------|------------|------|----|
| OAI-012 | **Import index store (corruption-resilient + vault-sync)** — `src/index/import-index.ts` + tests (2). Persists `{appleNotesId → {vaultPath, contentHash, importedAt, untrusted?: bool}}` via plugin `data.json`. Secondary key: content hash (collision flagged, not silently re-imported). Vault listeners: `app.vault.on('rename')` (update path) + `app.vault.on('delete')` (remove entry) registered via `registerEvent`. Readback contract: `JSON.parse(written)` round-trips losslessly AND structural shape matches expected schema. Corruption recovery: safe-degraded mode (delete-capable imports disabled until repaired). Repair scans frontmatter for `apple_notes_id` (default scoped to configured import folder; configurable to whole-vault); progress indicator if scan exceeds 2s. Repair conflict policy: duplicate id → ambiguous (delete disabled until resolved); missing path → drop entry + log; frontmatter/hash mismatch → rebuild with `untrusted: true`; repair report appended to receipt. Index repair follows atomic-rebuild pattern: build into staging, validate, atomic-swap. Never silently treats corruption as empty. | not started | OAI-002 | high | Adversarial tests pass: corrupt JSON triggers safe-degraded mode (Notice + "Repair" action); rename listener updates path; delete listener removes entry; repair correctly handles all 4 conflict cases (duplicate id, missing path, frontmatter mismatch, hash mismatch). Atomic-rebuild fault injection: simulated mid-repair crash leaves the previous index file intact. |

**M4 exit gate:** All adversarial tests pass.

## M5: Modal UI

| ID | Description | State | Depends On | Risk | AC |
|----|-------------|-------|------------|------|----|
| OAI-013 | Notes modal — `src/ui/NotesModal.ts`, `styles.css` (2). Lists notes (account/folder columns, search, filter); checkbox multi-select; preview snippet from body fetch; target-folder picker; dry-run toggle; "show already-imported" toggle (controls visibility AND checkbox-enable). Already-imported notes disabled by default with badge. Notes with attachments shown with informational badge `(N attachments — body only)`; selectable as normal. DOM via `createEl` (no `innerHTML` with user data). `styles.css` committed with ≥3-4 minimal selectors (modal table, row-disabled state, badge), included in release zip. **If OAI-024 spike showed >5s for 1k notes, modal MUST use paginated/streamed loading.** Cancel-after-current control during execution. May be developed with mock data initially. | not started | OAI-005, OAI-006, OAI-012, OAI-024 | medium | Modal renders mock data correctly (table, search, filter, multi-select, badges). Already-imported badge logic matches index state. Attachment badge correct. No `innerHTML` with user data (manual code grep + lint clean). `styles.css` committed. If contingency triggered, paginated/streamed loading verified against synthetic 1k-note mock. |
| OAI-014 | Confirm-delete modal — `src/ui/ConfirmModal.ts` (1). Lists N notes about to import + soft-delete. Explicit "Notes will be moved to Recently Deleted in Apple Notes" copy. **If any selected note has `attachmentCount > 0`, also displays: "X of these notes have attachments which will be lost after Apple's Recently Deleted retention expires."** Requires confirm click. | not started | OAI-013 | medium | Modal lists N notes; copy exact match. Attachment-loss warning appears when `attachmentCount > 0` for any selected, hidden when none. Confirm click resolves promise; cancel click rejects. |
| OAI-015 | Command + ribbon wiring — `src/main.ts` (1). "Browse Apple Notes" command palette entry; ribbon icon launches modal. Commands and ribbon registered ONLY when `Platform.isMacOS === true`. TCC probe runs on first command invocation per OAI-019. | not started | OAI-013, OAI-014, OAI-019 | low | Command + ribbon visible on macOS, absent off-platform. First-invocation triggers TCC probe path; subsequent invocations skip if already granted. Command name does NOT include "Apple Notes Import" prefix (per submission rule). |

**M5 exit gate:** Mock-data run-through end-to-end succeeds. Off-platform smoke confirms no command/ribbon registration.

## M6: Import Orchestrator (safety-critical)

| ID | Description | State | Depends On | Risk | AC |
|----|-------------|-------|------------|------|----|
| OAI-016a | **Import transaction model + sequencing (orchestration only)** — `src/import/transaction.ts` + tests (2). Implements canonical pipeline order per note: `fetch → convert → write markdown → persist index → invoke verify (OAI-016b) → invoke delete-gate (OAI-016c) → append receipt`. Receipt append is NOT a delete gate. Sequencing is the primary safety control. Function contract: `runNoteTransaction(note) → {status, errorReason?}` where `status ∈ {imported, imported-not-deleted, skipped, errored}`. 016a does NOT implement verify rules (those live in 016b). 016a does NOT invoke delete directly (gated by 016c). | not started | OAI-006, OAI-011, OAI-012 | high | Pipeline executes in declared order (test inspects call sequence via spies on each step). Function contract returns one of the 4 statuses for each branch. 016a does not import the osascript delete module (architectural test). |
| OAI-016b | **Composite verify implementation (pure verification)** — `src/import/verify.ts` + adversarial tests (2). Function contract: `verifyImport(expectations) → {ok: boolean, failures: string[]}`. Verify covers: (1) md exists, (2) md content matches expected pre-write hash (NOT a self-hash), (3) index entry persisted and JSON-roundtrips losslessly with structural shape match. No side effects beyond reads. | not started | OAI-011, OAI-012 | high | ≥4 adversarial scenarios pass: (a) md missing → ok:false, failures contains 'md-missing'; (b) md content mismatch (mocked partial-write) → ok:false; (c) index entry missing → ok:false; (d) index entry shape mismatch → ok:false. Happy path returns ok:true with empty failures. No side effects (verified by spy on writers). |
| OAI-016c | **Soft-delete gate execution** — `src/import/delete-gate.ts` (1). Function contract: `executeDeleteIfVerified(noteId, verifyResult) → {deleted: boolean, abortReason?}`. Gates the actual `osascript` delete behind `verifyResult.ok === true`. On `verifyResult.ok === false`, returns abort-with-reason without ever invoking delete. | not started | OAI-007, OAI-016b | high | Integration test with `verifyResult.ok === false` → confirms delete-not-called via spy on OAI-007 wrapper. With `verifyResult.ok === true` → delete invoked, returns `{deleted: true}`. |
| OAI-016d | **Batch execution + cancellation + progress** — `src/import/batch.ts` + UI wiring (2). Sequential execution: await each note's pipeline before starting the next (no parallel osascript→Notes). Per-note independence: a single note's failure aborts only that note's delete, never propagates. Cancel-after-current observed only between note transactions; once a note enters verify→delete, its pipeline runs to completion. Progress updates streamed to modal. Final summary dialog: imported / imported-not-deleted / skipped / errored counts + aggregate `attachments_dropped`. | not started | OAI-016a, OAI-016b, OAI-016c, OAI-013 | high | 3-note integration test (1 success, 1 verify-fail, 1 conversion-error) produces correct summary: 1 imported, 1 imported-not-deleted, 0 skipped, 1 errored. Only the success note appears in Recently Deleted in Apple Notes. Cancel-after-current test: cancel during note 2's pre-verify phase → note 2 finishes pipeline; note 3 not started. |
| OAI-016e | Receipt log writer — `src/import/receipt.ts` (1). Per-batch markdown written to receipts folder with imported / imported-not-deleted / skipped / errored sections + aggregate `attachments_dropped` line. Imported paths use `[[wikilinks]]`; skipped/errored entries have plain-text reason. Receipt filename collision → timestamp suffix. Receipt-write failure logs to console + Notice; does NOT roll back the soft-delete. | not started | OAI-016d | low | Receipt file written with correct sections. Wikilinks valid (point to real created files). Filename collision test: same-second second receipt produces a timestamp-suffixed name. Receipt-write failure (mocked) logs but does not throw or call delete-rollback. |

**M6 exit gate:** All adversarial tests pass + 3-note batch integration test produces correct outcomes.

## M7: Permission & Platform UX (parallel to M5/M6)

| ID | Description | State | Depends On | Risk | AC |
|----|-------------|-------|------------|------|----|
| OAI-019 | TCC permission probe + handler — `src/permissions/tcc.ts` + README guidance (2). Probe runs on first user-initiated command invocation (NOT plugin load). Settings-tab "Re-check permission" button is also a user-initiated probe. Denial detection: parse stderr for patterns from OAI-026 evidence (`Not authorized to send Apple events`, error codes -1743 / -10000); timeout fallback (treat hangs as probable denial); generic-error fallback (does NOT auto-disable on unmatched). On confirmed denial: in-app Notice + settings-tab guidance directs user to System Settings → Privacy → Automation → Obsidian → Notes; commands disabled until granted. | not started | OAI-004, OAI-026 | medium | Real-world test: `tccutil reset AppleEvents` → first command invocation surfaces correct guidance Notice + settings-tab guidance text. After granting in System Settings, "Re-check permission" button transitions to enabled state. Timeout fallback test: mock 30s hang → treated as probable denial. Generic-error fallback test: unmatched stderr does NOT auto-disable commands. |
| OAI-020 | Platform gate (no UI registration off-platform) — `src/main.ts` (1). `Platform.isMacOS === false` OR `Platform.isMobile === true` → `onload()` performs early-return BEFORE any side effects. Single Notice "Apple Notes Import: macOS only" emits **once per install/version** (de-dup via stored marker). | not started | OAI-002 | low | Off-platform smoke (mobile or stubbed `Platform.isMacOS = false`): no commands, ribbon, or settings-tab registered. Multiple reloads emit only one Notice (de-dup marker persists). On version bump, marker cleared and one new Notice emitted. |

**M7 exit gate:** TCC reset → first command surfaces correct guidance. Off-platform smoke confirms no UI registration AND only one Notice across multiple reloads.

## M8: Distribution

| ID | Description | State | Depends On | Risk | AC |
|----|-------------|-------|------------|------|----|
| OAI-021 | README, LICENSE, screenshots — `README.md`, `LICENSE`, `docs/screenshots/` (3). README documents: permission flow, soft-delete semantics + Recently Deleted retention, frontmatter fields (incl. `apple_notes_id` privacy notice prominent), **prominent v1-attachment-loss warning** per AC12, known limitations, manual install steps, TCC troubleshooting section, macOS-only banner. LICENSE chosen (MIT or 0-BSD). ≥2 screenshots: modal + confirm dialog. | not started | M6, M7 complete | low | README contains all sections listed. Attachment-loss warning visible in the first screen of the README (above the fold). LICENSE file present. ≥2 PNG screenshots committed under `docs/screenshots/`. |
| OAI-022 | Release workflow + asset inspection — `.github/workflows/release.yml` (1). Tag push → builds → attaches `main.js`, `manifest.json`, `styles.css` to GitHub release. Manifest fields match release tag/version. `.gitignore` excludes `main.js` + dev artifacts. **Release asset inspection step** (in workflow): zip contains exactly `manifest.json`, `main.js`, `styles.css`; fails if extra files (source maps, tests, dev files) detected. Submission self-critique checklist runs clean. | not started | OAI-021 | medium | Tag push to a test branch successfully publishes release with exactly the 3 assets. Asset inspection step fails on a deliberately-broken commit (e.g., adds `.map` file) — confirms the inspection step works. Self-critique checklist (lint + grep + manifest match) passes locally. |
| OAI-023 | Community submission PR — community-plugins.json entry, separate fork (1). Submission PR drafted against `obsidianmd/obsidian-releases`; submission self-critique checklist run clean. | not started | OAI-022 | low | Fork created. PR opened against `obsidianmd/obsidian-releases` with a single-line addition to `community-plugins.json`. Self-critique checklist re-run clean against the published release. |

**M8 exit gate:** Submission PR opened. Release asset inspection passes on real tag push. Self-critique clean.

## State Legend

- `not started` — task not begun
- `in progress` — task active
- `blocked` — task cannot proceed (note blocker in run-log)
- `done` — task complete (acceptance criteria met)
- `cancelled` — task removed (note rationale in run-log)
