---
project: opportunity-scout
domain: software
type: task-list
skill_origin: action-architect
created: 2026-03-14
updated: 2026-03-14
tags:
  - tasks
---

# Opportunity Scout — Tasks

## M0: Source + Scoring Validation

| ID | Description | State | Depends On | Risk | AC |
|----|-------------|-------|------------|------|----|
| OSC-001 | Extract scoring calibration data from v1–v7 research dispatches. Produce structured calibration seed (graveyard seed, high-scoring patterns, dimension weights, conflict-safety boundaries). Resolve U5 (research brief v0.5). | done | — | low | Calibration document exists in `design/` with: ≥3 rejected categories, ≥5 high-scoring patterns, conflict-safety boundary examples from v5. U5 resolved (brief located or reconstructed). |
| OSC-002 | Initialize external repo (`~/openclaw/opportunity-scout/`). Create SQLite schema for all 4 data contracts: source registry, candidate records, digest mapping, graveyard. WAL mode enabled. Scaffold config/secrets: `.env.example` with required vars (Telegram bot token, API credentials), startup validation that checks all required env vars before pipeline runs. | done | — | low | Repo exists with initial commit. SQLite migration creates all 4 tables matching spec schemas. WAL mode verified (`PRAGMA journal_mode` returns `wal`). `.env.example` lists all required env vars. Startup validation script exits non-zero if any required var is missing. Schema validation test passes. |
| OSC-003 | Research and populate M0 source registry. Identify ≥3 RSS sources per signal tier (T1: opportunity patterns, T2: demand signals, T3: landscape shifts) + HN API endpoint for M0 focus domains (AI/LLM, builder communities, creator economy). Confirm each source has a working ingestion path. | done | OSC-002 | low | Source registry contains ≥9 sources (≥3 per tier T1/T2/T3). Each source has: confirmed URL/endpoint, source_type, signal_tier, check_frequency, parser_config. Each source returns ≥1 normalized item in a dry-run adapter test or is explicitly marked inactive with reason. No sources requiring unsolved access (Reddit, Discord, freelance). |
| OSC-004 | Define adapter interface contract (input: source config → output: normalized items per spec schema). Implement RSS adapter porting FIF adapter pattern. Test against ≥1 M0 RSS source. | done | OSC-002, OSC-003 | low | Adapter interface defined and documented (types/contract file). RSS adapter ingests from ≥1 real source. Output conforms to Normalized Item Schema. Incremental polling via `last_item_seen` works (no duplicates on re-run). |
| OSC-005 | Implement HN API adapter using HN Firebase API. Handle rate limiting and incremental polling. Test against HN Show/Ask/Top stories. | done | OSC-002, OSC-003 | medium | HN adapter ingests from HN API. Output conforms to Normalized Item Schema (same interface contract defined in OSC-004). Rate limiting prevents API abuse. Incremental polling works (content_hash dedup prevents duplicate items). |
| OSC-006 | Curate 50-item triage test set: 20 items from v1–v7 research findings (known-good and known-bad), 20 items from FIF inbox, 10 synthetic edge cases (conflict-safety boundary, low-automation/high-economics, etc.). Each item has expected gate scores. | done | OSC-001 | low | Test set contains exactly 50 items with expected H/M/L scores for all 3 gates. Distribution: 20 v1–v7, 20 FIF, 10 synthetic. Edge cases include: ≥2 conflict-safety boundary items, ≥1 low-automation/high-economics item, ≥1 item that should be blocked by all 3 gates. |
| OSC-007 | Design three-gate triage scoring prompt. Run 50-item test through Haiku and Sonnet independently. Compute agreement rate (exact match on all 3 gate labels per item). Document fallback plan if <85%. | done | OSC-001, OSC-006 | medium | Triage prompt exists with deterministic rubric for each gate. Exact-match agreement ≥85% (≥43/50 items with identical H/M/L on all 3 gates). If <85%: fallback plan documented with cost impact (Sonnet-only at ~$12–18/month). Prompt stored in repo as versioned artifact. |
| OSC-008 | Implement candidate registry: CRUD operations, state machine (new→acknowledged/bookmarked→researching→evaluating→active/parked/rejected/killed), dedup via content_hash + canonical_pattern, graveyard negative-match filtering. Initialize graveyard from OSC-001 rejected categories. | done | OSC-001, OSC-002 | low | All state transitions enforced (invalid transitions rejected). Dedup correctly merges repeated patterns (sighting_count increments, source_urls appends, last_seen updates). Graveyard contains ≥3 entries from research. Graveyard negative-match prevents resurfacing of rejected patterns (unless reason_code invalidated). |
| OSC-009 | M0 integration test: ingest from ≥1 RSS source + ≥1 HN source → triage score items → insert qualifying items into candidate registry. End-to-end data flow validation. | done | OSC-004, OSC-005, OSC-007, OSC-008 | medium | Pipeline ingests from 2+ real sources, scores items via Haiku triage, inserts qualifying items into candidate registry. No errors. Dedup prevents duplicate candidates on re-run. Items blocked by triage gates are not inserted. |

## M1: First Digest Production

| ID | Description | State | Depends On | Risk | AC |
|----|-------------|-------|------------|------|----|
| OSC-010 | Build digest assembly pipeline: Sonnet-tier batch ranking of triage-passing items, key insight generation per item, digest template rendering, direct Telegram Bot API delivery, digest mapping rows written to SQLite. Vault archival of all digests (delivered + suppressed). Threshold-based suppression (AD-8). | done | OSC-007, OSC-008, OSC-009 | medium | Digest renders correctly in Telegram (formatted text, no truncation). Each key insight ≥50 chars and references ≥1 specific entity from the item (not generic filler). Digest mapping rows written (digest_id, item_index, candidate_id, telegram_msg_id). Suppressed digests archived in vault at `data/digests/`. Threshold-based suppression works (empty digest → no delivery, archive written). |
| OSC-011 | Add Discord webhook mirror for digest delivery. Implement weekly health heartbeat via Telegram Bot API (AD-9). Add pipeline failure alert (immediate, separate from weekly heartbeat). | done | OSC-010 | low | Discord receives digest mirror via webhook. Weekly heartbeat delivered (sources checked, items scanned, items qualified, digests delivered this week). Pipeline failure triggers immediate alert to Telegram. Heartbeat sends even if no digests that week. |
| OSC-012 | Build bash pipeline orchestration script: scan → score → digest → deliver → archive. Implement run IDs (timestamp-based), idempotent ingest (last_item_seen incremental polling), daily digest lock (one digest per calendar day). Add data retention: cleanup raw ingested items older than 30 days. | done | OSC-004, OSC-005, OSC-007, OSC-008, OSC-010 | high | Pipeline executes full scan→score→digest→deliver→archive cycle. Run ID generated per execution. Re-running same cycle produces no duplicate inserts or sends. Daily digest lock prevents double-digest. Data retention cleanup runs without error. |
| OSC-013 | Create LaunchAgent plist for daily pipeline. Configure error isolation (source failure doesn't block other sources, scoring failure suppresses digest with alert). Validate multi-model invocation: Haiku triage via OpenClaw + Sonnet digest via `claude --print --model sonnet`. | done | OSC-012 | high | LaunchAgent plist loads and fires on schedule. Source failure logged but other sources still process. Scoring failure suppresses digest and sends alert. Haiku triage + Sonnet digest both invoked successfully in a single pipeline run. |
| OSC-014 | Build Telegram Bot API feedback poller: long-running persistent process (LaunchAgent with KeepAlive, not cron — required for ≤60s response latency). Poll via getUpdates with offset management. Route recognized commands to parser. Send acknowledgement responses for all commands (success and error). | done | OSC-008, OSC-010 | medium | Poller runs as persistent LaunchAgent process. getUpdates offset managed correctly (no duplicate processing, no dropped updates on restart). Recognized commands routed to parser. Unrecognized input ignored gracefully. Acknowledgement sent for every command: success ("Bookmarked #2: [title]") and error ("Item #7 not found in latest digest", "Could not parse command"). Median response latency ≤60s over 10 test commands. |
| OSC-015 | Implement feedback command execution: !bookmark, !research, !pass, !reject via digest mapping resolution (most recent digest_id → item_index → candidate_id). Reply-to-message resolution for unambiguous targeting. /scout add [url] [context] creates new candidate. Throttle enforcement (1 research/day, 5 evaluations/month). State updates to candidate registry. | done | OSC-008, OSC-014 | medium | All 5 commands correctly update candidate state. Digest mapping resolution returns correct candidate for item number. Reply-to-message resolution works when Danny replies to a specific digest. Throttles enforced (research over-limit returns error with remaining quota). /scout add creates new candidate from URL with state=new. |
| OSC-016 | Pre-launch validation checklist. Run pipeline 3 times (at least 1 overnight run via LaunchAgent). Simulate source failure. Verify feedback commands. Verify heartbeat logic. Hours, not days — then ship to Danny and start the M2 clock. | done | OSC-013, OSC-014, OSC-015, OSC-011 | low | Pipeline ran ≥3 times with zero duplicate candidates or sends. ≥1 overnight run via LaunchAgent (not manual). ≥1 source failure simulated and isolated. ≥1 feedback command processed with correct state update + acknowledgement. Heartbeat logic verified (sends or would send on schedule). No false-positive alerts. |

## M2: Feedback Loop + Calibration

| ID | Description | State | Depends On | Risk | AC |
|----|-------------|-------|------------|------|----|
| OSC-017 | 30-day behavioral validation. Danny receives live digests starting day 1 after OSC-016 validation. Collect metrics: scan cycles completed, digests delivered, digests reviewed, bookmark/research rate, review time estimates. 21-day interim checkpoint for early signal. If pipeline breaks during M2, fix and resume (don't reset the clock unless >3 consecutive days lost). Assess against M2 gate criteria at day 30. If abort criterion met, document rationale and halt. | todo | OSC-016 | high | Metrics collected for full 30-day window. 21-day interim checkpoint documented. Gate assessment at day 30 with pass/fail per criterion: ≥10 scan cycles, ≥5 qualifying digests reviewed, bookmark/research rate ≥20%. **ABORT if <20% bookmark/research rate after 30 days OR <5 digests delivered in any 30-day period.** If abort: diagnosis documented (content problem vs. channel problem vs. habit problem). Pipeline fixes during M2 logged with downtime duration. |
| OSC-018 | Implement source yield scoring (yield_score per source, rolling 30-day). Apply deprioritization policy (30 days zero→weekly, 90 days zero→flag). Tune triage threshold based on M2 feedback data. Update scoring prompt if feedback disagreement rate >30% (bookmarked items that triage scored low, or passed items that triage scored high). | todo | OSC-017 | low | yield_score computed and stored for each source. ≥1 source priority adjustment made based on data. Triage threshold adjusted ≥1 time from M0 baseline. Scoring prompt updated if feedback disagreement rate >30%, or explicit note that no miscalibration detected. |

## M3: Monthly Evaluation

| ID | Description | State | Depends On | Risk | AC |
|----|-------------|-------|------------|------|----|
| OSC-019 | Build monthly evaluation memo generator. SQLite aggregation queries for: signal summary, portfolio status, top signals, landscape shifts, behavioral impact. Template implementation. Produce first monthly memo with Danny. | todo | OSC-018 | low | First memo produced. Contains: signal summary with counts, portfolio status, ≥3 top signals ranked, scoring model notes with ≥1 calibration observation. Evaluation took ≤30 minutes (system generates raw data in ≤5 min; remaining time is Danny's review). Data sourced from SQLite aggregation (not manual counting). |
| OSC-020 | Design Execute Mode feedback interface. Define four-metric schema (time-per-task, revenue, friction log, market response). Describe integration pattern. Provide concrete Wisdom Library example. Design artifact only — no implementation. | todo | — | low | Schema documented in `design/`. Integration pattern described (how stream data flows into Scout scoring). ≥1 concrete example showing Wisdom Library metrics flowing into Scout. |
